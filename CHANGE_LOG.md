---
title: Relay Notes - Change Log
date: 2026-06-08
updated: 2026-06-11
tags:
  - altered-craft
  - changelog
status: living
---

# Relay Notes — Change Log

Running narrative of what shipped and why. Append new entries at the end of the latest date section; create a new dated section when the date rolls over.

This is the *narrative* record. For *what's planned* see [planning/notes.md](./planning/notes.md). For *transcription dial design rationale* see [planning/transcription-tuning.md](./planning/transcription-tuning.md). For *empirical accuracy outcomes* see notes.md § V1.1 accuracy tuning.

Entry style: bold lead-in summarizing what shipped, then the *why* / non-obvious context. One bullet per shippable unit. Keep it terse — file paths and identifiers are welcome; comma-prose isn't.

---

## 2026-06-08

- SwiftUI "hello world" skeleton builds and runs on iPhone 15 Pro Max (Developer Mode enabled, device signed and trusted). V1.0 done.
- Project layout: `Relay Notes/Relay Notes/` holds source (`Relay_NotesApp.swift`, `ContentView.swift`, `Assets.xcassets`). Planning docs live at `Relay Notes/planning/` as a folder reference (not a target member).
- **Scope decision:** v1 is voice-to-text only. All LLM-driven cleanup, organization, and categorization moved to "Later." Doc restructured into v1 / Later sections.
- **V1.1 first pass shipped to device.** Mic → AVAudioRecorder (.m4a/AAC 64 kbps mono) → SpeechAnalyzer + SpeechTranscriber (file-based) → SwiftData. Functionally working end-to-end on iPhone 15 Pro Max. Accuracy on "some words" — tuning underway, see "V1.1 accuracy tuning" below.
- **Deviation from plan:** Used `AVAudioRecorder` instead of `AVAudioEngine` for V1.1. Reason: `AVAudioRecorder` writes AAC/m4a natively; `AVAudioEngine → file` requires `AVAudioConverter` middleware. `AVAudioEngine` returns in V1.2 when we need raw PCM buffers for streaming live transcription.
- **No tests yet:** No test target exists. Production code is structured to be testable (DI + protocols). Add a Unit Testing Bundle via Xcode (File → New → Target) before V1.2.
- **Tuning sheet shipped.** All four accuracy knobs are runtime-configurable from the in-app Tuning sheet (slider icon, top-right). `Tunings` (`@Observable`) holds state; `RecorderViewModel` reads a snapshot at the moment of recording/transcribing and passes `RecordingOptions` / `TranscriptionOptions` to the recorder/transcriber. Protocols updated to take options.
- **Tunings persistence shipped.** `Tunings` properties have `didSet` writes to `UserDefaults`; `init` reads back on launch. Settings survive cold starts.
- **V1.1 marked done.** Done-when criterion (speak → stored transcript offline) is satisfied. Accuracy tuning continues in parallel.
- **V1.2 step 1 shipped — notes list.** `ContentView` refactored to a `NavigationStack` with `NotesListView` on top (driven by `@Query` sorted by `createdAt` desc) and `RecorderView` at the bottom (Voice Memos pattern). Tapping a row navigates to a `NoteDetailView` placeholder. Tuning gear moved to the navigation bar.
- **V1.2 step 2 shipped — audio playback in `NoteDetailView`.** New `Audio/AudioPlayer.swift` is a `@MainActor @Observable` wrapper around `AVAudioPlayer`. Detail view shows: timestamp, selectable transcript, then a fixed bottom toolbar with a scrub slider, play/pause button, and elapsed/total time labels. Playback session uses `.playback` category. Player stops on `onDisappear`. Polling loop is a `Task` instead of `Timer` to keep Swift 6 strict concurrency clean.
- **V1.2 step 3 shipped — delete.** `Note.deleteWithAudio(in:)` removes the audio file from disk and the SwiftData row. Wired to swipe-to-delete on the list (no confirmation — swipe gesture is the confirmation) and a trash button in detail view (confirmation alert, since dismissing the screen is a less obvious "you're about to delete" cue). Mode default also changed: `.measurement` → `.default` based on real-world finding that AGC-disabled recordings are too quiet for comfortable playback.
- **V1.2 step 4 shipped — search.** `.searchable` on the navigation stack, in-memory case-insensitive filter on `note.transcript`. Empty-state uses `ContentUnavailableView.search(text:)` to distinguish "no matches" from "no notes." In-memory rather than SwiftData `#Predicate` because `localizedCaseInsensitiveContains` isn't reliably supported in `#Predicate` and at v1 dataset sizes the filter cost is negligible.
- **Debug tooling shipped.** `SampleNotes.seed/deleteAll` and `Tunings.resetToDefaults` exposed as a `#if DEBUG` "Debug" section in the Tuning sheet. Lets us populate the simulator without needing a phone for UX testing.
- **All four V1.2 essentials shipped: list, detail + playback, delete, search.** Daily-driver bar in reach. Three stretch items remain (rename, share, live streaming) — pickable based on what you actually miss in daily use.
- **End-to-end validation in iOS Simulator.** Seeded 12 sample notes via debug action, validated list rendering, search (term match + no-match empty state), swipe-to-delete, trash-button + confirmation flow, detail view (transcript displays; playback shows graceful "couldn't load" error for synthetic notes with no real audio). Console log reviewed — all noise is system/simulator-side (haptic plist absent in simulator, `.searchable` internal constraints, etc.); nothing from our code.
- **V1.2 status:** essentials done. Top-level V1.2 checkbox stays open until daily-use validation on the phone confirms the done-when ("reach for this daily instead of stock Voice Memos"). Stretches are open and prioritization-dependent.
- **Framing pivot — local-first / cloud-opt-in / model-choice axis made explicit.** Doc updated so "offline" and "Apple" are no longer conflated. Three transcriber paths (Apple Speech on-device with no model choice, Local ASR on-device with your-choice models via MLX/whisper.cpp, Cloud STT opt-in only). Same pattern for LLM: local-MLX is the default, cloud is opt-in, Apple Foundation Models are an optional fourth engine (intentionally not primary — Apple is behind the open-source ecosystem on capability as of mid-2026). Pipeline diagram, intro callouts, spine, transcription tier table, inference-engine intro, and architecture notes all updated.
- **Next options:** (a) carry the build on the phone for a day or two and let actual usage decide what stretch to take, (b) pick a small simulator-testable stretch — **Rename** (smallest, real value) or **Share**, (c) bigger: **Live streaming** (`AVAudioEngine` swap-in — phone-only to validate), (d) jump straight to **V1.3 TestFlight ship**.
- **V1.2 stretch shipped — share transcript text.** `ShareLink` added to `NoteDetailView` toolbar (`.topBarTrailing`, beside the trash button), sharing `note.transcript` with `note.displayTitle` as subject (nicer Mail/Messages experience). Disabled when the transcript is whitespace-only. Audio-file sharing intentionally out of scope — text-only was the explicit ask.
- **V1.2 stretch shipped — live streaming transcription.** Architectural swap: `AudioRecorder` (`AVAudioRecorder`) → `LiveAudioEngine` (`AVAudioEngine` + input tap). The tap writes AAC/m4a to disk via `AVAudioFile(forWriting:settings:)` *and* hands PCM buffers (converted via `AVAudioConverter` to the analyzer's `bestAvailableAudioFormat`) into an `AsyncStream`. `Transcriber` protocol gained `makeStreamingSession(options:)`; `AppleSpeechTranscriber`'s session wraps `SpeechAnalyzer.start(inputSequence:)` and exposes a cumulative partial-transcript `AsyncStream<String>`. `RecorderViewModel` has new states (`.recording(partial:)`, `.finalizing`); `RecorderView` renders live partials in a scrollable card above the record button. File-based `transcribe(_:options:)` retained for the future cloud STT path. Validation: builds clean; needs phone-mic testing for real validation.
- **Streaming session now unions `.volatileResults` into user preset.** Phone-test surfaced: with default `.transcription` preset, the live partial card stayed empty (preset emits no volatile/partial results). Switching to `.progressiveTranscription` worked but brings `fastResults` along, costing accuracy. Fix in `AppleSpeechTranscriber.makeStreamingSession` — build the streaming `SpeechTranscriber` from the user's preset's `transcriptionOptions`/`reportingOptions`/`attributeOptions` and *union* `.volatileResults` into reporting options. Live partials regardless of preset; finalized accuracy of the stored transcript unchanged.
- **New planning doc — [planning/transcription-tuning.md](./planning/transcription-tuning.md).** Documents every transcription dial (UI + hidden), what each does, current default + why, plus the streaming-vs-file-based accuracy discussion and a decisions log. The V1.1 accuracy tuning section in notes.md remains the *empirical* record (what we observed when we turned the knobs); the new doc is the *design* record (what the knobs are and why we picked their defaults).
- **V1.2 closed.** All four essentials shipped (list, detail + playback, delete, search) and all three stretches shipped (rename, share, live streaming). Top-level V1.2 checkbox flipped to done. Next: V1.3 — TestFlight ship to personal device.
- **App icon shipped.** Designed via nano-banana (voice→text relay motif: white waveform bars flowing into horizontal text lines on a teal→indigo gradient), composed in Icon Composer, exported as `RelayNotes-Icon.icon`, added to project and wired up via Target → General → App Icon field. System auto-handles Default / Dark / Tinted appearance variants from the single source. Replaces the default empty `AppIcon.appiconset` (still present in `Assets.xcassets`, but ignored).
- **V1.3 reframe.** Original V1.3 was "TestFlight ship to personal device" — but TestFlight requires the paid Apple Developer Program ($99/yr) and the user is on the free tier. Done-when ("home screen + daily use") is actually achievable via Developer Mode sideloading, no enrollment needed. Reframed V1.3 around sideloading-and-dogfooding (7-day re-sign cycle, data persists). Created new V1.4 for TestFlight, explicitly **gated on L1 completion** — committing to the paid program is tied to validating that on-device MLX inference actually works on the phone (concrete capability trigger, not a date or a default). Intro callout (the "Three constraints" `[!info]` block) updated to reflect the new sideload-default framing.
- **Change log split out.** Moved the running narrative from `planning/notes.md` into this file (`CHANGE_LOG.md` at repo root) so `notes.md` becomes the design/plan doc and `CHANGE_LOG.md` is the time-ordered ship record. Added a `CLAUDE.md` rule so future sessions append here after each shippable change.

## 2026-06-09

- **Fixed — no transcription while screen locked (field bug).** Recording then locking the screen captured nothing: the live partial froze (showed `…` on unlock) and the locked window was missing from both transcript and saved audio. Root cause: the app declared no `audio` background mode, so iOS *suspended the process* on lock and the `AVAudioEngine` input tap stopped firing (it resumes on unlock — matching the reported symptom exactly). The `planning/notes.md` watch-item already flagged this ("Background recording … you need the background audio mode and a long-lived audio session"). Fix details worth knowing: `INFOPLIST_KEY_UIBackgroundModes` is a **no-op** — `UIBackgroundModes` isn't in Xcode's allowlist of generatable Info.plist keys (verified: the key never reached the built plist). So added a partial `Relay Notes/Info.plist` holding `UIBackgroundModes = [audio]` and set `INFOPLIST_FILE` in both build configs while keeping `GENERATE_INFOPLIST_FILE = YES` — Xcode merges the generated keys on top of that base file. The target uses a file-system-synchronized group, which auto-added the new `Info.plist` to Copy Bundle Resources → "Multiple commands produce Info.plist"; resolved with a `PBXFileSystemSynchronizedBuildFileExceptionSet` excluding `Info.plist` from membership. The audio session was already `.playAndRecord` and stays active for the whole recording, satisfying the "long-lived session" half. Verified: clean build, merged `Info.plist` contains `UIBackgroundModes=[audio]` *and* retains all generated keys (microphone/speech usage, orientations). **Needs on-phone confirmation** that locked-screen capture now works.
- **Known related gap (not fixed here).** No `AVAudioSession` interruption handling — a phone call, alarm, or another app grabbing audio mid-recording still stops `AVAudioEngine` with no auto-resume. Distinct from the suspension bug above; track as a follow-up if it bites in daily use.
- **Interruption handling shipped (closes the gap above).** `LiveAudioEngine` now observes `AVAudioSession.interruptionNotification` and emits an `AsyncStream<InterruptionEvent>` (`.began` / `.resumed` / `.stopped`) alongside the buffer stream. On `.began` the system has already stopped the engine; on `.ended` we honor the `shouldResume` hint — reactivate the session and `engine.start()` again (tap stays installed, so capture flows back into the *same* file and transcription session), or report `.stopped` when the hint says no or restart throws. `RecorderViewModel` gained a `.paused(partial:)` state: `.began` → paused (frozen partial), `.resumed` → recording, `.stopped` → auto-finalize so the captured audio isn't lost (no manual-resume affordance in the tap-to-record UI). `RecorderView` shows an orange "Paused — interrupted" status and keeps the partial card visible; the record button finalizes from `.paused` too. The interruption observer is delivered on `queue: .main` and bridged to the MainActor via `MainActor.assumeIsolated`; the observer/stream are torn down in `engine.stop()`.
- **Latent crash fixed in passing.** `LiveAudioEngine.stop()` now calls `removeTap(onBus:)` unconditionally instead of only when `engine.isRunning`. An interruption-stopped engine reports `isRunning == false` but still holds its tap, so the old guard would have left a stale tap and crashed the *next* `installTap`. `removeTap` is a no-op when no tap is installed, so unconditional removal is safe.
- **Still open (smaller).** Route changes (`AVAudioEngineConfigurationChange` / `routeChangeNotification` — e.g. unplugging headphones mid-record) aren't handled; the backgrounded-during-a-call edge needs on-device validation. Build is clean (Debug, no warnings); interruption behavior needs a real phone-call/Siri test to confirm.
- **First test target shipped — `Relay NotesTests` (Swift Testing).** The project's first unit-test target, hosted in the app (`@testable import Relay_Notes`). Covers the interruption → state mapping: `RecorderViewModel.handleInterruption` was refactored to delegate to a pure, static `nextState(for:from:)` so the transitions (`.began`→paused, `.resumed`→recording, and the guard/no-op cases) are testable without constructing the view model or touching hardware. 5 tests, all green via `xcodebuild test`. Added the target with the `xcodeproj` Ruby gem (not by hand — it round-trips the Xcode 16 synchronized groups safely) and validated the whole flow on a `/tmp` repo copy first. Scheme `Relay Notes` is now **shared** so the test action runs from the CLI. `CLAUDE.md` updated: testing how-to + the "edit the project via the gem, validate in /tmp" rule.

## 2026-06-10

- **T1 plan locked — Local Whisper via MLX, promoted ahead of the L stages.** Originally the Tier-2 transcription upgrade was "post-v1, when Apple's accuracy is the bottleneck *and* staying local matters" with no L-stage assigned. Decision today: do it next, before L1. Rationale: proving on-device third-party-model viability is the riskiest unknown of the local-first thesis, and the runtime we wire here is the same one L1+ LLMs will need on Apple silicon. New "Transcription upgrades" section in `planning/notes.md` between v1 and the L stages, with three items: **T1** (Local Whisper via MLX, next), **T2** (second on-device engine — Parakeet-MLX or Qwen3-ASR), **T3** (`CloudTranscriber`, opt-in). Tier-2 row of the transcription table relabeled model-agnostic ("Local ASR via MLX").
- **Picked raw `mlx-swift` over WhisperKit for T1.** Two reasons: (1) keeps the app to a single ML runtime — picking WhisperKit (Core ML under the hood) would split the runtime surface the moment L1's MLX LLMs land; (2) pays the MLX-on-iOS learning cost on a smaller problem (Whisper inference) than an LLM, with the lessons transferring directly to Parakeet-MLX / Qwen3-ASR / L1 model loading. Escape valve: drop to WhisperKit behind the same `Transcriber` protocol if `mlx-swift` proves intractable on iOS (~2 weeks of evening effort as the trigger). Documented in `planning/notes.md` (T1 risks watch-item) and `planning/transcription-tuning.md` (Decisions log).
- **T1 design decisions locked.** Default model: `mlx-community/whisper-small.en-mlx` (~250 MB, English-only). Stored in Application Support (not Caches — eviction risk), excluded from iCloud backup. Pre-download + delete affordances in Settings to preserve the offline-recording promise once installed. `TranscriptionOptions` becomes a sum type (`.apple` / `.whisperMLX`) so per-engine options stay type-safe; new `TranscriptionEngine` enum + `TranscriberFactory` resolves the engine per recording. First cut is **finalize-only** (no streaming partials) — recorder shows "Transcript will appear when you stop recording" placeholder + elapsed time + level meter while Whisper is the selected engine; `.finalizing` runs the full-file decode with a "Transcribing…" spinner. Buffer strategy: in-memory PCM during `feed`, decode once at `finish()` (~115 MB resident for 30 min of audio on iPhone 15 Pro Max — comfortable).
- **Issue #1 opened to track the in-memory buffer revisit trigger.** github.com/AlteredCraft/relay-notes/issues/1. Triggers to revisit: notes regularly exceed ~20 min, memory-pressure warnings under dogfood, or device scope expands beyond the iPhone 15 Pro Max. Options at revisit time documented (scratch WAV during `feed`, reuse the AAC writer's file, or chunked decode).
- **Planning docs updated.** `planning/notes.md`: new "Transcription upgrades" section (T1/T2/T3), tier-2 table row, v1 architecture-notes Transcriber bullet refreshed (concrete class names: `WhisperMLXTranscriber`, `CloudTranscriber`; sum-type plan flagged), new T1 risks watch-item block (raw `mlx-swift` choice + escape valve, in-memory buffer ceiling + issue #1, 250 MB download UX). `planning/transcription-tuning.md`: scope callout points at T1; new "Tier 2 — Local ASR via MLX (Whisper)" section with planned dials (model variant, language), hidden defaults (in-memory buffer, greedy decoding, no streaming partials), recording UX, model lifecycle; "Streaming vs file-based" gains a Tier 2 subsection noting Whisper collapses both paths to a file-style decode; Decisions log gains five 2026-06-10 entries.
- **T1 sliced into T1.0–T1.3.** Coarser-grained than V1.2's micro-list — each slice retires a distinct risk and is independently shippable. **T1.0** Provider plumbing (sum-type `TranscriptionOptions`, `TranscriptionEngine`, `TranscriberFactory`, picker scaffolded with Whisper disabled) — derisks the abstraction refactor in isolation from MLX. **T1.1** Whisper inference proven in isolation via a `#if DEBUG` smoke button — derisks `mlx-swift` on iOS hardware before touching the recorder. **T1.2** Whisper wired into the recorder flow with model download UX, no-streaming placeholder, model-missing guard. **T1.3** On-device measurements (load/decode/RAM/battery), tests (smoke + streaming session contract), decisions log entries; revisits defaults if numbers demand it. Sub-items live in `planning/notes.md` under the T1 bullet.
- **T1.0 shipped — provider plumbing complete, Apple Speech unchanged.** `TranscriptionOptions` is now a sum type (`.apple(AppleSpeechOptions)` / `.whisperMLX(WhisperMLXOptions)`); `AppleSpeechTranscriber` pattern-matches and `preconditionFailure`s on engine mismatch (defense-in-depth — should never fire if factory and engine stay aligned). New types: `TranscriptionEngine`, `WhisperModelVariant` (with `huggingFaceRepoID` / `displayName` / `approxDownloadMB` ready for T1.2). `Tunings` adds `engine` + `whisperModelVariant` UserDefaults-backed properties and an injectable `UserDefaults` parameter so the new persistence tests don't contaminate `.standard`. `TranscriberFactory` (`@MainActor`, caches instances per engine) replaces the direct `AppleSpeechTranscriber()` injection in `RecorderViewModel`; the view model now resolves per recording, snapshot-read from `tunings.engine`. `RecorderViewModel.startRecording` gained a catch for `TranscriptionError.engineNotImplemented` (new error case) that surfaces the underlying message to the user. `SettingsView` gains a "Transcription engine" section — Apple Speech selectable, "On-device (Whisper)" visible with a "Coming next" tag (no picker control because SwiftUI's `Picker` doesn't support per-row disable; built it as button + read-only row instead). `WhisperMLXTranscriber` stub throws `engineNotImplemented` from both protocol methods. **Tests:** 10 new (`TuningsPersistenceTests` 6 cases covering defaults + round-trip + transcriptionOptions sum-shape + resetToDefaults; `TranscriberFactoryTests` 4 cases covering engine→impl mapping + instance caching). Project mutation (adding two test files to the `Relay NotesTests` target — which is a regular `PBXGroup`, not a synchronized one) done via the `xcodeproj` Ruby gem; validated on a `/tmp` repo copy before applying to the real project per the CLAUDE.md rule. All 15 tests green (`xcodebuild test … iPhone 17 Pro`). Build clean.
- **T1.1 split into T1.1a + T1.1b after research surfaced two wrong assumptions in the original plan.** (1) `mlx-swift-examples` has **no** Whisper reference (issue #146 closed unanswered) — the actual reference is `ml-explore/mlx-examples/whisper/mlx_whisper/` Python, meaning T1.1's "vendor pieces" is in fact a port of `audio.py` + `tokenizer.py` + `whisper.py` + `decoding.py` to Swift against `MLX + MLXNN + MLXRandom`, not a copy-paste. (2) `whisper-small.en` at FP16 (~481 MB resident) likely needs the `com.apple.developer.kernel.increased-memory-limit` entitlement, which a free-tier sideload profile (V1.3) may strip — a real collision with the V1.4 gate. Mitigation: T1.1b defaults to `whisper-tiny.en` (~75 MB) to sidestep the entitlement question entirely until T1.2. T1.1a derisks the SPM dep + Metal-on-device link in one evening before any porting effort. `planning/notes.md` T1 bullet rewritten (mlx-swift-examples → mlx-examples Python port), T1.1 expanded into T1.1a/T1.1b sub-checkboxes, new T1 risks watch-item for the entitlement collision. `planning/transcription-tuning.md` Tier-2 model default switched to `tiny.en` (T1.1b) → `small.en` (T1.2, conditional); two new Decisions log entries for the split and the model swap.
- **T1.1a shipped — mlx-swift dep added, MLX-hello smoke button in Tuning → Debug.** `mlx-swift` SwiftPM package added to the app target (products `MLX`, `MLXNN`, `MLXRandom`, pin `from: "0.31.0"` → resolved 0.31.4 + transitive swift-numerics 1.1.1). Project mutation done via the `xcodeproj` Ruby gem (`/tmp/add_mlx_swift_dep.rb`), validated on a `/tmp` repo copy (build + 15-test run both green) before applying to the real project per the CLAUDE.md rule. New `Transcription/MLXSmoke.swift` (`#if DEBUG`, `nonisolated enum` for Swift 6 strict concurrency) prints `GPU.deviceInfo()` (architecture / maxBufferSize / maxRecommendedWorkingSetSize / memorySize) and the result of `sum(MLXRandom.normal([4,4]) @ MLXRandom.normal([4,4]))` to the Xcode console. Wired to a new "Run MLX smoke (console)" button in `SettingsView`'s existing `#if DEBUG` section, dispatched via `Task.detached(priority: .userInitiated)`. **Build prereq surfaced:** Xcode 26 ships the Metal toolchain as a downloadable component (`xcodebuild -downloadComponent MetalToolchain` — ~688 MB, one-time) which mlx-swift's Cmlx target needs to compile its Metal shaders. Build clean (3.4 s incremental). **Done-when for T1.1a still requires on-device validation on the iPhone 15 Pro Max** — simulator is documented to crash on MLX import (insufficient `MTLGPUFamily`), so the button is only useful on device.
- **T1.1a validated on iPhone 15 Pro Max.** `architecture=applegpu_g16p` (A17 Pro GPU), `maxBufferSize=4 GB`, `maxRecommendedWorkingSetSize≈5.43 GB`, `memorySize≈7.66 GB`. Smoke matmul produced a non-zero float, no crash. T1.1a done-when satisfied. Baseline numbers worth holding: the ~5.43 GB working set leaves comfortable room for tiny.en (~75 MB weights, expect ~200–300 MB resident) without the increased-memory-limit entitlement, but the iOS process-level jetsam limit is a separate cap that the Metal numbers don't reveal — T1.2's small.en (~250 MB / ~481 MB FP16) may still trip it. The entitlement question stays open for T1.2.
- **T1.1b research surfaced two scope adjustments before any code.** (1) `mlx-swift`'s `loadArrays(url:)` only reads `.safetensors` — Python `mlx.load(...)` handles both `.npz` and safetensors but the Swift binding doesn't, so both the HF `weights.npz` and the `mel_filters.npz` from mlx-examples need converting at staging time. (2) `MLX.PadMode` only has `.constant` and `.edge` — no `.reflect`, so the Whisper STFT's reflect padding has to be implemented manually (via `arange`-built gather indices). Also noted: BPE *encoder* is NOT needed for greedy inference (primes with fixed `sot`+`notimestamps` IDs, emits IDs), so the Swift tokenizer in T1.1b-2 drops from ~398 Python lines to ~80 — decode-only.
- **T1.1b-1 shipped — WhisperAudio mel pipeline + asset staging.** Ports the relevant subset of `ml-explore/mlx-examples/whisper/mlx_whisper/audio.py` (constants, `padOrTrim`, `hanning`, reflect-padded `stft`, `logMelSpectrogram`) to `Transcription/WhisperAudio.swift` (`nonisolated enum` namespace, ~170 lines). Replaces Python's `load_audio` ffmpeg shell-out with `AVAudioFile` + `AVAudioConverter` (one-shot whole-file decode → 16 kHz mono Float32 `[Float]`, caller wraps in `MLXArray`). Output `[nFrames, nMels] = [3000, 80]` for a 30-s chunk @ 16 kHz, matching Python. STFT uses `asStrided` for overlap-add framing without copies; reflect padding built from `arange(padding, 0, step: -1)` + `arange(n)` + `arange(n-2, n-2-padding, step: -1)` concatenated into a gather index. **Asset staging:** `Relay Notes/Resources/whisper-tiny.en/` ships `config.json`, `mel_filters.safetensors` (converted from npz at staging time, ~167 KB), `gpt2.tiktoken` (~835 KB), `ls_test.flac` (~150 KB LibriSpeech sample — well-known transcript = unambiguous done-when later). `weights.npz`/`weights.safetensors` are **gitignored** (~75 MB) and fetched via `scripts/fetch-whisper-tiny.sh` → `scripts/convert-whisper-assets.py` (PEP 723 inline-deps via `uv`, converts npz dicts to safetensors). T1.2 will replace this bundle-everything pattern with an in-app URLSession download into Application Support.
- **Default model promoted to `whisper-small.en` — entitlement empirically not required.** Right after T1.1's tiny.en validation, swapped the bundled config + weights to small.en (~481 MB FP16 safetensors) and re-ran the smoke. **Process did not get killed by jetsam** — the `com.apple.developer.kernel.increased-memory-limit` entitlement is *not* required for whisper-small.en on the iPhone 15 Pro Max, contradicting our pre-experiment hypothesis (and the original T1 risks watch-item). Free-tier sideload is unaffected. V1.4 TestFlight is no longer Whisper-gated on this concern — the entitlement question still applies to L1+ MLX LLMs (3–4B Q4 ≈ 2.5 GB) but Whisper alone fits comfortably. **Accuracy delta on `ls_test.flac`:** tiny.en said `' Then the goods sold openly, shorted the burden she had borne so long in secret and bravely trudged on alone.'` (two nonsense word substitutions); small.en said `' Then the good soul openly shouldered the burden she had borne so long in secret, and bravely trudged on alone.'` (correct phrase + correct comma). The two tiny.en errors ("goods sold"→"good soul", "shorted"→"shouldered") were exactly the noise-floor-tier mistakes we'd expected to clear up at the larger size. **Timing on warm runs:** model load 16→53 ms, encoder forward 60→419 ms (~7× — matches 3× layers × 2× width = 6× compute), total transcribe 491→1772 ms (~3.6×). ~3.4× real-time on a 6 s clip — still comfortable for the finalize-only "Transcript will appear when you stop recording" UX planned for T1.2. **Repo plumbing:** renamed `Resources/whisper-tiny.en/` → `whisper-small.en/`, refactored `scripts/fetch-whisper-tiny.sh` → `scripts/fetch-whisper-model.sh [tiny.en|small.en]` (defaults to small.en, also fetches config.json so the dimensions auto-match), updated `.gitignore` to cover the new path, updated `WhisperModelTests.configLoadsFromBundle` to pin small.en's 768/12/12 dims, updated CLAUDE.md, `planning/notes.md` watch-item (struck the entitlement collision), and `planning/transcription-tuning.md` Decisions log + Tier 2 default dial.
- **T1.1 done — Whisper transcript on iPhone 15 Pro Max.** Validated end-to-end 2026-06-10. Output on `ls_test.flac`: `' Then the goods sold openly, shorted the burden she had borne so long in secret and bravely trudged on alone.'` — fluent English, correct sentence structure, correct capitalization, correct punctuation. A few words look like the canonical `tiny.en` accuracy noise floor ("goods sold" probably should be "good sleep"; "shorted" probably should be "shortened") — model size, not port bug; `small.en` would clean those up. **Timing on the warm run:** model load 16 ms, encoder forward 60 ms, decoder + tokenizer 415 ms, total 491 ms for ~6 s of audio (~12× real-time). Cold first-run is the 5–10 s range dominated by Metal shader JIT (see prior note from T1.1b-3). **T1.1 done-when satisfied** — `planning/notes.md` T1.1/T1.1a/T1.1b checkboxes ticked. T1 (top-level Local Whisper via MLX) is now partially done — T1.2 (recorder wiring + model-download UX) and T1.3 (on-device measurements + tests) remain. **Escape valve unused:** the 2-week WhisperKit fallback threshold from the watch-item didn't trigger — the raw `mlx-swift` port worked first try on device, in one session.
- **T1.1b-4 shipped — greedy decode + `WhisperMLXTranscriber.transcribe(_:options:)` end-to-end.** New `Transcription/WhisperDecoding.swift` (~120 lines, `nonisolated enum`) ports the minimal subset of `mlx_whisper/decoding.py`: prime with `[sot, notimestamps]`, feed the prime once, then loop one new token per call threading the per-block KV cache, take `logits[:, -1]`, apply two additive masks (`SuppressTokens` baked in as a literal `Set<Int>` of 84 IDs; `SuppressBlank` for `space=220` and `eot=50256` at step 0 only), argmax, stop on `eot` or `n_text_ctx/2 = 224` steps. Drops the ~620 lines of Python that handle beam search, sampling, language detection, timestamps, temperature fallback, and the compression-ratio heuristic — none of which are in scope for greedy English-only T1.1b. **`nonSpeechTokens` provenance:** new `scripts/compute-non-speech-tokens.py` (PEP 723 inline-deps via `uv`, tiktoken on Python) ports `Tokenizer.non_speech_tokens` from the Python reference — recreates the English-only Whisper encoding, encodes the hardcoded symbol list, sorts the result. Output baked in as a Swift literal under `nonSpeechTokens` with the script path in a comment; re-run if the vocab ever changes. **Transcriber wiring:** `WhisperMLXTranscriber.transcribe(_:options:)` now does `PCM → padOrTrim → logMelSpectrogram → asType(.float16) (matches weight dtype) → batch dim → embedAudio → greedyDecode → tokenizer.decode → return string`. **No caching yet:** model + tokenizer reload on every call (~30 ms model + JIT on first call). Kept that way for T1.1b to avoid the actor-isolation gymnastics that `Sendable` Transcriber + cached `MLXArray` state would require; T1.2's recorder wiring will resolve caching. The streaming path still throws `engineNotImplemented` — recorder integration is T1.2's problem. `MLXSmoke.run()` is now `async` and extended with a `runWhisperTranscribe()` block that times the full transcribe pipeline against `ls_test.flac` and prints the transcript. All 26 simulator-eligible tests still green. **Pending on-device confirmation** that the transcript matches the expected LibriSpeech text.
- **T1.1b-3 validated on iPhone 15 Pro Max.** `audio features shape = [1, 1500, 384]` — exactly what the encoder should produce (batch=1, n_audio_ctx=1500 after the stride-2 conv halves the 3000-frame mel, n_audio_state=384). **Timing on `ls_test.flac` (~6 s clip):** model load 27–35 ms, encoder forward **1174 ms cold → 59 ms warm** (20× speedup once Metal's shader cache populates). The cold-run delta is dominated by MLX's runtime Metal shader compilation — produces a long stream of `Warning: Compilation succeeded with:` blocks complaining about unused `constexpr` constants in mlx-swift's own shader headers (`defines.h`, `unary_ops.h`, `ternary_ops.h`). These are upstream cosmetic warnings from mlx-swift, not anything we caused; they appear on every mlx-swift app's first launch and stop once the kernel cache is warm. **Implication for T1.1b-4 budget:** at ~59 ms warm encoder + an estimated ~5–6 ms per decoder step × ~30 tokens for the ls_test clip, full-clip greedy transcription should land well under 1 s on warm runs (cold runs will be slower as more decoder-side kernels JIT).
- **T1.1b-3 shipped — WhisperModel (encoder + decoder + KV cache + weight load).** New `Transcription/WhisperModel.swift` (~330 lines) ports `mlx_whisper/whisper.py` to mlx-swift. **Notable wrinkles:** (1) mlx-swift's built-in `MultiHeadAttention` uses parameter key names `query_proj`/`key_proj`/`value_proj`/`out_proj` and a single bias flag, but the Whisper safetensors uses `attn.{query,key,value,out}` with per-projection bias (key has no bias). Solution: custom `WhisperAttention` Module that names its projections to match. (2) Swift 6 strict concurrency infers `@MainActor` on Module-subclass synthesized inits, which conflicts with mlx-swift's nonisolated parent. Solution: `nonisolated final class X: Module` on every Module subclass. (3) The decoder's `positional_embedding` is a learned `MLXArray` parameter (not an `Embedding` layer); declared via `@ParameterInfo(key:)` so the safetensors key matches. (4) The encoder's `_positional_embedding` is precomputed sinusoidal — declared the same way but the safetensors has no matching key, so `update(parameters:)` leaves it alone. (5) Per-block KV cache type is `((MLXArray, MLXArray)?, (MLXArray, MLXArray)?)` for `(self-attn KV, cross-attn KV)`; cross-attn computes K/V from the encoder output once and reuses for every decoder step. **Weight loading:** `loadFromBundle()` calls `loadArrays(url:)` on `weights.safetensors`, drops the `alignment_heads` entry (we don't port word timestamps), unflattens via `ModuleParameters.unflattened(_:)`, and calls `model.update(parameters:)`. **Asset fetch:** `scripts/fetch-whisper-tiny.sh` ran end-to-end (75 MB download → safetensors conversion via `scripts/convert-whisper-assets.py`); 167 arrays unpacked across encoder/decoder. **Tests:** 1 new simulator-safe test (`configLoadsFromBundle` — pins n_mels=80, n_audio_state=384, n_audio_layer=4, n_vocab=51864, etc. to whisper-tiny.en's values). Model-load + encoder-forward validation moves to `MLXSmoke.run()` device path — prints load time, dims, audio features shape, encoder time. Pbxproj mutation via `xcodeproj` Ruby gem (`/tmp/add_whisper_model_tests.rb`), validated on `/tmp` copy. All 26 tests in 6 suites green.
- **T1.1b-2 shipped — WhisperTokenizer (decode-only BPE).** New `Transcription/WhisperTokenizer.swift` (`nonisolated final class WhisperTokenizer: Sendable`, ~110 lines including the special-token ID table). Parses bundled `gpt2.tiktoken` (50256 raw-byte BPE ranks, `<base64> <rank>` per line) into a `[Data]` indexed by ID. Exposes the special-token IDs the greedy decoder needs (`eot=50256`, `sot=50257`, `transcribe=50358`, `notimestamps=50362`, etc.) and the prime sequence (`[sot, notimestamps]` for the English-only, no-timestamps configuration). `decode(_ ids: [Int]) -> String` skips specials, concatenates the BPE byte sequences, and runs one lossy UTF-8 decode at the end so multi-byte characters that cross BPE boundaries reassemble correctly. **Encoder intentionally omitted** — greedy inference primes with fixed IDs and emits IDs, so the ~250-line Python BPE encoder path is dead weight here; if a future re-transcription path needs encode, it gets added then. **Tests:** 7 simulator-safe cases (`Relay NotesTests/WhisperTokenizerTests.swift`) covering vocab load, special-token positions, prime-sequence shape, ASCII single-byte decode (ranks 0..4 → `!"#$%`), special-token filtering, empty input, out-of-range IDs. Pbxproj mutation via the same `xcodeproj` Ruby gem pattern as T1.1b-1 (`/tmp/add_whisper_tokenizer_tests.rb`), validated on a `/tmp` copy. All 25 tests green.
- **T1.1b-1 validated on iPhone 15 Pro Max.** Smoke output: `mel_80` filters `[80, 201]`, `ls_test.flac` decoded to 106 640 samples (≈6.7 s @ 16 kHz), `padOrTrim` → `[480000]`, `logMelSpectrogram` → `[3000, 80]` with values in `[-0.427, 1.573]` — diff exactly 2.0, which is the invariant the Python reference guarantees (the floor is `max - 8` in log10 space, divided by 4 after normalization → range ≤ 2). Caught and fixed an over-tight upper-bound assertion in `endToEndLogMelFromBundledFLAC` while at it (the test was asserting `maxVal ≤ 1.0`; the actual invariant is `max − min ≤ 2.0`). Mel pipeline ready to plug into the tokenizer + model in T1.1b-2/3.
- **Resources are flat-bundled, MLX tests are simulator-blocked.** Xcode's file-system-synchronized group flattens the `Resources/whisper-tiny.en/` subdir at build time — `Bundle.main.url(forResource:withExtension:)` lookups *omit* the `subdirectory:` argument (single-variant for now; revisit if filenames collide). Confirmed empirically that **any MLX op crashes the iOS Simulator test runner** (matches the prior research about insufficient `MTLGPUFamily`). New convention in `Relay NotesTests/Whisper*Tests.swift`: simulator-safe checks (constants, precondition error throwing, AVFoundation-only) at the top, MLX-using shape/sanity checks gated behind `#if !targetEnvironment(simulator)`. Numerical validation moves to `MLXSmoke.run()` which now exercises each Whisper sub-pipeline on the iPhone 15 Pro Max and prints to the Xcode console — extended in this commit to print `mel_filters` shape, `ls_test.flac` PCM length, `padOrTrim` shape, and `logMelSpectrogram` shape + min/max values. **Tests:** 3 new simulator-safe (`constantsMatchPythonReference`, `melFiltersRejectsUnsupportedBinCount`, `loadPCMFromBundledFLAC`) + 6 device-only (gated `padOrTrim*`, `hanning`, `melFilters`, `logMelSpectrogramShape*`). All 18 simulator-eligible tests green. Pbxproj mutation via `xcodeproj` Ruby gem (`/tmp/add_whisper_audio_tests.rb` — sets explicit `path` + `name` since the test target is a regular `PBXGroup`), validated on `/tmp` copy first. CLAUDE.md updated with the asset-bundling and simulator-guard rules.
- **T1.2 sliced into T1.2a–T1.2f.** Substages from the earlier "one big bullet" T1.2 plan: **T1.2a** loaders accept a directory `WhisperModelLocation` (next), **T1.2b** `WhisperModelStore` (URLSession download → Application Support, integrity check, no-iCloud-backup), **T1.2c** `WhisperMLXTranscriber` becomes an `actor` with cached `WhisperModel` + `WhisperTokenizer`, **T1.2d** `makeStreamingSession` (in-memory PCM accumulate, decode at `finish()`), **T1.2e** Settings activates Whisper row + pre-download / delete affordance, **T1.2f** recorder placeholder UX (no live partial; elapsed + level meter) + blocked-record guard when model is missing. Substage order picked so each is independently shippable — T1.2a is mechanical with no UI risk, T1.2c–d retire the actor-isolation/caching design call before the UI lands on top.
- **T1.2a shipped — Whisper loaders parametrize by `WhisperModelLocation`; `WhisperModelVariant` deleted.** New `Transcription/WhisperModelLocation.swift` (`nonisolated enum`, two cases: `.bundled` resolves via `Bundle.main.url(forResource:withExtension:)`, `.directory(URL)` resolves via `dir.appendingPathComponent("<name>.<ext>")` with a `FileManager.fileExists` guard). `ModelDimensions.loadFromBundle()` → `load(from: WhisperModelLocation)`; `WhisperModel.loadFromBundle()` → `load(from:)`; `WhisperTokenizer.init()` → `init(location:)`; `WhisperAudio.melFilters(nMels:)` → `melFilters(nMels:from:)` and `logMelSpectrogram(audio:nMels:padding:)` → `logMelSpectrogram(audio:nMels:padding:from:)` (threaded explicitly — no default — so every call site declares its location). `WhisperMLXTranscriber` gained a `location: WhisperModelLocation` stored property (default `.bundled` keeps the dev path unchanged) that flows through all loader calls; T1.2b's download manager will inject `.directory(applicationSupportURL)`. **Cleanup:** dropped `WhisperModelVariant` enum entirely (only `small.en` is supported — user confirmed tiny.en isn't needed; if a second variant ever returns we re-add the enum); dropped `Tunings.whisperModelVariant` property + UserDefaults key + the `resetToDefaults` line; dropped `WhisperMLXOptions` struct (was `modelVariant: WhisperModelVariant + language: String` — both dead now) and collapsed `TranscriptionOptions.whisperMLX(WhisperMLXOptions)` → bare `case whisperMLX`. **Pre-existing Swift 6 isolation warnings on `ModelDimensions` and the global `sinusoids(...)` function fixed in passing** (both marked `nonisolated`) since I was touching `WhisperModel.swift` anyway and the surrounding code already uses that pattern (`nonisolated final class WhisperAttention/...Model`). **Tests:** 2 new simulator-safe (`locationDirectoryReturnsNilForMissingFile`, `locationDirectoryReturnsURLWhenFileExists`); 2 removed (`whisperModelVariantDefaultsToSmallEN`, `whisperModelVariantRoundTripsThroughUserDefaults`); 6 updated call sites (`WhisperTokenizerTests.tokenizerLoadsFromBundle`, `WhisperAudioTests.{melFiltersRejectsUnsupportedBinCount,melFiltersLoadFromBundle,logMelSpectrogramShapeFromZeroAudio,endToEndLogMelFromBundledFLAC}`, `WhisperModelTests.configLoadsFromBundle`). All 26 simulator-eligible tests green; build clean (4.2 s).
- **T1.2b shipped — `WhisperModelStore` (download/presence/delete, Application Support, SHA-256 integrity).** Pivot from earlier "host weights on GitHub Releases" plan to **download directly from HuggingFace**: `mlx-community/whisper-small.en-fp16` already ships `model.safetensors` (481 MB FP16) with the MLX-native tensor naming our `WhisperModel.load(from:)` expects — same file we'd have been hosting, but at the authoritative source with no maintenance on our side. Spotted while spot-checking HF per user prompt (the prior CHANGE_LOG note about "mlx-swift only reads safetensors, mlx-community ships npz so we convert" was correct for the `*-mlx` sibling repo but missed the `*-fp16` sibling). New `Transcription/WhisperModelStore.swift` (`@MainActor @Observable`, ~430 lines): `Status` enum (`missing` / `downloading(progress)` / `ready` / `failed(reason:)`), `download() async throws`, `stageBundledAssets()` (copies bundled `config.json` + `gpt2.tiktoken` + `mel_filters.safetensors` into `Application Support/whisper/small.en/` — small files stay bundled and version-locked with the Swift port, only weights are downloaded), `delete() throws`, `cancelDownload()`, `var location: WhisperModelLocation { .directory(modelDirectory) }`. **URL pinned to commit SHA** `f8ff44ec66c4b1748fb2e3eb13b3b521a0bdfea8` (not `main`) so the file content at the URL is immutable; integrity verified via streaming `CryptoKit.SHA256` against `36375db96d2900eceb62ed9fe43dec23854266adc6d7ad827f661b81fb0893b4` (HF's `lfs.oid`) plus expected-size early-fail. **Download infrastructure:** private `DownloadCoordinator` (`URLSessionDownloadDelegate`, `@unchecked Sendable`, `NSLock.withLock` for Swift 6 strict-concurrency compliance) owns a custom `URLSession` so `didWriteData(...)` progress callbacks fire (the simpler `URLSession.download(for:delegate:)` async API only delivers `URLSessionTaskDelegate` callbacks, which doesn't include download progress); `withTaskCancellationHandler` plumbs `Task.cancel()` through to the download task; session is `invalidateAndCancel`'d on completion to break the delegate retain cycle. Downloaded file gets `isExcludedFromBackup = true`. **Tests:** 8 new simulator-safe (`WhisperModelStoreTests`): empty dir → missing, present weights → ready, location points at modelDirectory, `stageBundledAssets` copies all three files + is idempotent (re-stages over tampered copies), `delete` removes everything and flips status, `sha256Hex` matches `CryptoKit.SHA256.hash` reference, download-URL pin to commit SHA (catches accidental un-pinning). Pbxproj mutation via `xcodeproj` Ruby gem (`scripts/add_whisper_model_store_tests.rb`); validated on `/tmp` repo copy first per CLAUDE.md rule. All 34 tests in 7 suites green. **`MLXSmoke.run()` extended** with a `runWhisperStore()` block that prints the on-disk state + exercises `stageBundledAssets` on device — no network download in the smoke (that's T1.2e's UI button when it lands). **Integrity story validated against the real HF file (off-device).** Downloaded the pinned URL to `/tmp` and computed SHA-256 with `shasum -a 256` → `36375db96d2900eceb62ed9fe43dec23854266adc6d7ad827f661b81fb0893b4`, exact match. The HEAD response also exposes both candidate hashes as headers — `x-linked-etag` (LFS `oid`, what we want) vs `x-xet-hash` (chunk-store identifier) — and confirms the linked-etag pattern matches what HF documents as the file SHA-256. **Tensor-naming compatibility validated too:** parsed the safetensors JSON headers of both the HF file and our hand-converted local `weights.safetensors`; both contain exactly 479 tensors with identical key sets and matching shapes/dtypes on the spot-checked subset. So `WhisperModel.load(from: .directory(...))` will load the downloaded HF file without any remapping work. Only the on-device URLSession-actually-hits-the-URL part is unvalidated, and that's a routine network exercise post-T1.2e. **Production wiring** (transcriber → store) deferred to T1.2c.

## 2026-06-11

- **Dev workflow moved from Xcode's built-in assistant to Claude Code (terminal).** The Xcode assistant proved inefficient; everything except signing, previews, Instruments, and on-device `MLXSmoke` console reading now runs from the terminal. Intel gathered while validating the switch: the `xcode-tools` MCP server CLAUDE.md references is Apple's `mcpbridge`, shipped in Xcode 26.5 as `xcrun mcpbridge` — a stdio bridge that requires a *running* Xcode instance (handshake confirmed; register per-project with `claude mcp add --scope project xcode-tools -- xcrun mcpbridge`). Full CLI loop validated: `xcodebuild test` on the iPhone 17 Pro simulator ran all 34 tests / 7 suites green in ~12 s with zero Xcode UI involvement. Installed `xcbeautify` (Homebrew) for readable xcodebuild output. CLAUDE.md `## Build & validate` rewritten with the new premise, the MCP registration command, validated CLI commands, and the remaining Xcode-GUI-only tasks (7-day free-tier signing renewal, previews/Instruments, `MLXSmoke`).
- **T1.2c shipped — `WhisperMLXTranscriber` is now a caching `actor` wired to `WhisperModelStore` (device timing validation pending).** Conversion from `nonisolated final class` → `actor`: the loaded `WhisperModel` + `WhisperTokenizer` + `mel_filters` `MLXArray` live in a `LoadedAssets` struct cached across calls (was: fresh load per `transcribe`, ~30 ms + Metal shader JIT), with actor isolation making the non-Sendable MLX state safe behind the `Sendable` `Transcriber` protocol. Cache is **single-entry by design** — keyed by `WhisperModelLocation`, replaced (old model released first) when the location flips, since two resident `small.en` weight sets would be ~1 GB fp16 on an 8 GB device. **Store wiring:** transcriber takes an optional `WhisperModelStore`; new `store.activeLocation` computed property (`status == .ready ? location : nil` — lives on the store so the `Status` comparison stays on the main actor) is re-read on *every* transcribe with fallback `.bundled`, so download/delete mid-session takes effect next call. `TranscriberFactory.init` gained `whisperModelStore:` (default `nil`); ContentView construction/DI deferred to T1.2e per the slicing. `WhisperAudio.logMelSpectrogram` split into a location-loading convenience + a `filters:`-taking core so the cached filterbank is actually used. **Isolation findings (the non-obvious part):** the project's `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` infers `@MainActor` onto the actor's *synchronous init* — methods keep their actor isolation, verified empirically with a deliberate compile-error probe (`error: call to actor-isolated instance method 'assets(at:)'`, proving decode runs on the actor's executor, not the main thread). Both opt-out spellings are **rejected by the Xcode 26.5 toolchain**: `nonisolated init` ("invalid on an actor's synchronous initializer") and `nonisolated actor` ("modifier cannot be applied"). Resolution: accept the main-actor init (every construction site — factory, tests, smoke — is `@MainActor` anyway) and document it on the type; `MLXSmoke.runWhisperTranscribe` became `@MainActor` to match and now runs a **cold-then-cached double pass** printing both timings, which is the T1.2c done-when evidence to read off the device console next time the phone is plugged in. **Tests:** new `WhisperMLXTranscriberTests` — 5 simulator-safe (fallback resolution with no store / store-not-ready / store-ready, mid-session delete flips resolution back to `.bundled`, factory injects the store end-to-end) + 1 device-gated cache-identity probe (an actor *extension* in the test target so the non-Sendable assets never cross the isolation boundary); 2 new `WhisperModelStoreTests` for `activeLocation`. Test file wired into the pbxproj via new `scripts/add_test_file.rb` (generalizes T1.2b's one-off script — takes the filename as an argument; T1.2d will need it again), validated on a `/tmp` repo copy first per the CLAUDE.md rule. All 41 tests / 8 suites green; the two `WhisperAudio.swift` AVFoundation-Sendable warnings visible in a clean build predate this change (full-rebuild artifact, closure at `loadPCM`'s converter is untouched).
- **T1.2c isolation finding corrected — root cause was the implicitly-`@MainActor` protocol, not the actor init; fixed with `nonisolated protocol`.** The previous entry's claim ("`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` infers `@MainActor` onto an actor's synchronous init") was wrong — researched per user prompt. [SE-0466's exemption list](https://github.com/swiftlang/swift-evolution/blob/main/proposals/0466-control-default-actor-isolation.md) explicitly excludes "all declarations inside an `actor` type, including … initializers" from default isolation, and a minimal repro confirms it: a bare actor with a synchronous init compiles clean under `swiftc -default-isolation MainActor` (Swift 6.3.2). The actual chain: **protocols are *not* on the exemption list**, so unannotated `protocol Transcriber: Sendable` became implicitly `@MainActor`, and the long-standing conformance-inference rule ("conform to a global-actor protocol in your primary declaration → inherit that global actor") propagated `@MainActor` onto the actor's init — the compiler note names it outright (`note: main actor isolation inferred from conformance to protocol`) once you reproduce outside the Xcode build. (The `Sendable`-refinement exemption — "primary definition directly conforms to a protocol that inherits `SendableMetatype`" — covers conforming *types*, not protocols refining `Sendable`; hence `TranscriptionOptions: Sendable` et al. were never affected.) **Fix:** `nonisolated protocol Transcriber` + `nonisolated protocol TranscriptionSession` in `Transcriber.swift` — correct semantically, the protocols are isolation-neutral by design (conformers pick their own isolation: nonisolated class, actor, and T1.2d's session class to come). Fallout removed: `MLXSmoke.runWhisperTranscribe` dropped its workaround `@MainActor`; `WhisperMLXTranscriber`'s "init is `@MainActor`" doc rewritten (no markers needed — actor members really are exempt now that the protocol is nonisolated). `AppleSpeechSession` (unannotated, was silently `@MainActor` via the same conformance inference) compiles clean as nonisolated — its stored state is structurally Sendable, and its async members run on the caller's executor (`NonisolatedNonsendingByDefault`), which for the `@MainActor` `RecorderViewModel` caller is the main actor as before — no behavior change. Related upstream report of the same confusion: [swiftlang/swift#82870](https://github.com/swiftlang/swift/issues/82870). All 41 tests / 8 suites green, zero new warnings; nonisolated-context construction of the actor compiles silently (the original symptom, gone).
- **T1.2c device-validated — cache works on the iPhone 15 Pro Max; T1.2c closed.** `MLXSmoke` run with `small.en` bundled: cold transcribe 2165 ms → cached 1580 ms on `ls_test.flac` (~6 s clip), transcript byte-identical to T1.1's validated text. The 585 ms delta is the per-call `WhisperModel` + tokenizer + filterbank load the cache now eliminates — the earlier "~30 ms reload" estimate was a `tiny.en` figure (as is T1.1's oft-quoted 491 ms warm total; `small.en` is ~5× the model, not comparable). 1580 ms cached ≈ encoder (554 ms, measured standalone in the smoke's model section) + ~25-token greedy decode, i.e. pure compute with no reload — the T1.2c done-when as written. Note the smoke's "cold" understates a true app cold start: earlier smoke sections (matmul, mel, encoder) pre-pay most Metal shader JIT; T1.3 measures the real scenario. Store block on device looks right: `Application Support/whisper/small.en/` created, `stageBundledAssets` staged all three small files, status `missing` (no downloaded weights — bundled fallback in use, as designed pre-T1.2e). **Scoping flag raised for T1.2d:** `transcribe` is single-window — `WhisperAudio.padOrTrim` truncates at 30 s, so recordings longer than 30 s would silently lose everything past the first window. The Python reference (`mlx_whisper/transcribe.py`) handles this with a seek-loop over 30-s chunks; ours was scoped out in T1.1b. Must be addressed (in T1.2d or a dedicated substage) before Whisper is the daily-driver engine — voice notes routinely exceed 30 s.
- **T1.2d-1 shipped — chunked transcribe: timestamp-guided 30-s seek loop, model-agnostic windowing (device validation pending).** Closes the silent-truncation gap flagged at T1.2c validation (`padOrTrim` cuts at Whisper's architectural 30-s encoder window — `n_audio_ctx = 1500` positional embeddings, nothing our code chose). **Abstraction (per user requirement):** new `Transcription/ChunkedTranscription.swift` — `AudioWindow` (sampleRate + samplesPerWindow; `.whisper` = 16 kHz × 480 000) + `ChunkedTranscription.run(pcm:window:decodeWindow:)`, a model-agnostic driver that walks PCM in window-sized slices and advances by whatever boundary the per-window decode reports (`WindowAdvance.fullWindow` / `.toTime(seconds)`); a future model with different constraints supplies a different `AudioWindow` + closure, the loop never changes. Guards a non-positive `.toTime` by degrading to plain windowing (no stalls). **Whisper side (`WhisperDecoding` rework):** timestamp-mode greedy decode — primes `[sot]` (new `WhisperTokenizer.sotSequence`; `primeSequence`/`notimestamps` priming gone), per-step structural timestamp rules as pure `timestampRuleMaskRanges` (pairing, monotonicity, initial-timestamp cap `max_initial_timestamp = 1.0 s`) + the probabilistic timestamps-beat-text rule in MLX, `no_speech_prob` read at the SOT position pre-masking, `avg_logprob` accumulated from the masked distribution (reference formula `sum/(len+1)`), suppress set completed with the special tokens the T1.1b port missed (`sot`/`translate`/`transcribe`/`startoflm`/`startofprev`/`nospeech`). `parseWindow` ports the reference's segmentation: consecutive timestamp pairs → complete segments + `.toTime` seek to the last boundary (unfinished tail dropped, re-decoded next window); `single_timestamp_ending` → window fully consumed; no pairs → keep all, full-window advance. Window-level silence skip: `no_speech_prob > 0.6` unless `avg_logprob > −1.0` (reference defaults). **Deliberate deviation, documented in code + tuning doc:** ported OpenAI's `ApplyTimestampRules` monotonicity semantics — the `mlx-examples` version collects token *indices* where it needs *values* (`timestamps = [i for i, v in enumerate(seq) …]` then `mask[…, timestamp_begin : timestamps[-1]]`, an empty range), silently no-oping "timestamps shouldn't decrease". **Deliberately not ported:** `condition_on_previous_text` (repetition-loop source; its safety net is temperature fallback, which greedy-only doesn't have — windows decode independently), word timestamps, temperature fallback. **Tests:** 14 new simulator-safe in `WhisperChunkingTests` (driver: single/short window, mid-window `.toTime` restart math, zero-advance termination guard, empty-text joining, empty-PCM; parse: all four reference cases; mask rules: initial-step forcing + cap, pairing both directions, monotonicity; tokenizer: `timestampSeconds`). 55 tests / 9 suites green; `/tmp` copy validated first per rule. **`MLXSmoke` gained `runWhisperChunked`:** tiles `ls_test.flac` ~6× to ~36 s, writes a temp Float32 WAV, runs the *public* `transcribe(_:options:)` path — expect the sentence ~6×, two windows, timestamp-guided restart. That smoke run is the T1.2d-1 done-when. `MLXSmoke` needed an explicit `import AVFoundation` (`MemberImportVisibility` is on — transitive imports don't resolve).
- **Two audio-file bugs fixed off the first T1.2d-1 device run (smoke writer lifecycle + `loadPCM` short reads).** The chunked smoke failed on device with `buffer.frameCapacity != 0` from `loadPCM`. **Bug 1 (smoke-only):** `AVAudioFile` finalizes the WAV header on deallocation, and `runWhisperChunked` kept the writer alive while `transcribe` re-opened the URL — the reader saw length 0. Reproduced all three lifecycle variants in a standalone macOS script (writer-alive → length 0; scoped dealloc or explicit `close()` → full length); fixed by scoping the writer in a `do` block. **Bug 2 (production-relevant, caught by the new regression test):** the simulator round-trip then came back 256 frames short (31 744 of 32 000) — `file.length` was correct on disk, but a single `AVAudioFile.read(into:)` call returned short under parallel test load (full-suite runs failed 3/3; the isolated test passed — exactly 31×1024 frames, an I/O-quantum boundary). `loadPCM` trusted one `read` call to deliver the whole file on *both* its paths, so the converter path (m4a → future re-transcribe action) could silently drop audio tails under load. Rewrote `loadPCM` to chunked loop-reading (64 Ki-frame buffer) until `framePosition` reaches `file.length`, with a no-progress guard against header-overclaiming files; the converter path now streams chunks through `AVAudioConverter`'s input block (the canonical pattern) instead of one giant buffer — also dropping the whole-file-sized allocation. `@preconcurrency import AVFoundation` in `WhisperAudio.swift` per the compiler's own suggestion (the SDK marks the converter input block `@Sendable`; it runs synchronously inside `convert(to:)`) — clears the two pre-existing Sendable warnings too. New simulator test `writtenWAVRoundTripsThroughLoadPCM` pins the write→read pattern with a content spot-check. 56 tests / 9 suites, 3 consecutive full-suite runs green, zero warnings from our sources. Chunked smoke still **pending device re-run**.
- **T1.2d-1 device-validated — seek loop works on the iPhone 15 Pro Max; T1.2d-1 closed.** Tiled ~40 s clip (6 × 6.67 s) → transcript contains the sentence **exactly 6× complete** in 9311 ms (~4.3× realtime). The boundary math confirms timestamp-guided seek did its job: window 1 (0–30 s) held 4 complete repetitions + a partial 5th, and window 2 picked up at the last segment boundary (~26.7 s) — naive 30-s slicing would have severed repetition 5 mid-sentence; nothing was lost or mangled. Decode time scales with tokens-per-window as expected (fuller windows than the 6 s smoke clip → ~1.8 s/window cached doesn't extrapolate linearly to audio duration; T1.3 measures 1-min/5-min notes properly). Punctuation variance vs the no-timestamps decode ("Then," vs "Then") noted — same words, different decode mode, harmless.
- **T1.2d-2 shipped — `WhisperStreamingSession` (PCM accumulate → finalize-only decode); `makeStreamingSession` no longer throws `engineNotImplemented`.** New `Transcription/WhisperStreamingSession.swift`: `nonisolated final class` whose accumulator is a `Synchronization.Mutex<[Float]>` — all stored properties are immutable `Sendable` values, so the class is structurally `Sendable` with no `@unchecked` or hand-rolled locking. `audioFormat` reports 16 kHz mono Float32, which makes `LiveAudioEngine`'s existing `AVAudioConverter` deliver mel-pipeline-ready buffers during recording — zero resampling work at finish time. `feed(_:)` appends samples under the mutex (safe from any caller isolation); `updates` completes without ever yielding (the zero-partials contract — T1.2f replaces the live-transcript card with a placeholder); `finish()` snapshots and frees the buffer, runs the new `WhisperMLXTranscriber.transcribePCM(_:)` (extracted so the file path and the session share the T1.2d-1 chunked loop), and mirrors `AppleSpeechSession` by throwing `noSpeechDetected` on an effectively-empty transcript so the recorder's "didn't hear any speech" flow works identically across engines; `cancel()` drops the buffer. Memory bound: ~3.84 MB/min of Float32 PCM (~115 MB for a 30-min note — the issue #1 revisit trigger stands). The missing-model preflight guard is deliberately *not* here (T1.2f puts it in `RecorderViewModel.startRecording`); dev builds fall back to bundled weights. **Tests:** 5 new simulator-safe in `WhisperMLXTranscriberTests` (session type from factory method, native-format contract, multi-buffer accumulation via the `bufferedSampleCount` seam, empty-buffer no-op, cancel clears + updates-completes-without-yielding). 61 tests / 9 suites green. **Validation note:** the done-when (record → stop → Whisper transcript in a `Note`) needs Whisper to be *selectable*, which is T1.2e's Settings row — T1.2d-2 stays `[~]` until that manual pass.
- **T1.2e shipped — Settings activates the Whisper engine row + on-device model download/delete UI; the store is now constructed and threaded through the app.** The "On-device (Whisper) — Coming next" placeholder in `SettingsView` became a real selectable engine row (Button → `tunings.engine = .whisperMLX`, checkmark when active, mirroring the Apple Speech row). New `Views/WhisperModelSection.swift` (`WhisperModelSection`) renders the model lifecycle straight off `WhisperModelStore.status`: `.missing` → "Download model (≈480 MB)"; `.downloading(progress)` → `ProgressView(value:)` + Cancel; `.ready` → green "Installed" + destructive Delete; `.failed(reason)` → generic actionable copy + "Try again". **Wiring (the T1.2c-deferred DI):** `ContentView` now owns a single `@State WhisperModelStore()` and hands the *same instance* to both `TranscriberFactory(whisperModelStore:)` (so `activeLocation` resolution sees downloads/deletes) and `SettingsView(whisperStore:)` (so the UI drives them) — one store, two consumers, no env indirection. **Download survives sheet dismissal:** the button kicks an *unstructured* `Task` (not `.task`), since the store outlives the Settings sheet and progress should keep running if the user closes it; status is the single source of truth, so `download()`'s thrown error is logged via `OSLog` (subsystem `alteredcraft.Relay-Notes`) and otherwise discarded — the UI re-renders off `.failed(reason:)`. **Error-copy discipline (per `CLAUDE.md`):** `failureMessage(for:)` maps each `FailureReason` to generic, actionable copy and deliberately drops the diagnostic detail the reason carries — the HTTP status code (`.server`) and the missing asset name (`.bundledAssetMissing`) stay in logs, never the UI. **Tests:** 4 new simulator-safe in `WhisperModelSectionTests` pinning that contract (every reason → non-empty copy; `.server(503)` copy omits "503"; `.bundledAssetMissing("config.json")` omits "config"/".json"; non-cancel reasons read as actionable). 65 tests / 10 suites green. **Unblocks T1.2d-2's manual done-when** (record → stop → Whisper transcript in a `Note`) — Whisper is now selectable; the missing-model record-time guard + recorder placeholder UX remain T1.2f.
- **T1.2e + T1.2d-2 device-validated on the iPhone 15 Pro Max — both closed.** End-to-end pass: the real 481 MB `model.safetensors` downloaded from HuggingFace (progress UI live, integrity check passed, status flipped `.missing` → `.ready`), Whisper became selectable, a recorded note transcribed **accurately** into the saved `Note` (the first on-device proof that the T1.2d-2 finalize-only streaming path + T1.2d-1 chunked seek loop produce a correct transcript through the real recorder flow, not just the `MLXSmoke` harness), and Delete removed the model and flipped the Settings row back to the download button. This is the done-when for both substages — T1.2d-2 (`stopAndTranscribe` succeeds against `WhisperMLXTranscriber`) and T1.2e (download / select / delete on device). Only **T1.2f** (recorder placeholder UX while Whisper is selected + the record-time `.ready` preflight guard) remains in the T1.2 slice. Quantified load/decode/memory/battery numbers for 1-min and 5-min notes are still **T1.3**, not this pass — this was a functional pass (it works, transcript is right), not a measured one.
- **Transcript provenance — `NoteDetailView` now shows which engine/model produced each note.** New optional `Note.transcriptionModel: String?` (additive → SwiftData lightweight migration, no `MigrationPlan`; `nil` for pre-existing notes, which the detail view simply omits — no backfill since provenance was never recorded historically). The label is captured at save time from a new `TranscriptionSession.modelDescription` protocol member, keeping with the provider-abstraction spine: **the session is the authority on what model it loaded**, so it reports its own identity rather than `RecorderViewModel` inferring it from the engine enum. `AppleSpeechSession.modelDescription` → `TranscriptionEngine.apple.displayName` ("Apple Speech"); `WhisperStreamingSession` → new `nonisolated static WhisperMLXTranscriber.modelDescription` ("Whisper (small.en)") — a single source of truth flagged to become location-derived when multiple Whisper variants land. `RecorderViewModel.stopAndTranscribe` reads `session.modelDescription` (off the local `guard let session` binding, before `self.session` is cleared) into the `Note`; `NoteDetailView` renders it under the date as a `Label("Transcribed with …", systemImage: "waveform")`, shown only when present. **Tests:** new `NoteTests` (provenance defaults to `nil`; round-trips through an in-memory `ModelContainer`) + `sessionReportsWhisperModelProvenance` in `WhisperMLXTranscriberTests` (session reports the Whisper descriptor, contains "small.en"). 68 tests / 11 suites green.
- **Bug fix — engine selection now respects model presence; deleting Whisper's model no longer leaves it the active engine.** Surfaced by dogfooding: with Whisper selected, deleting the model flipped the Settings model row back to "Download model" (delete *did* work — it removed `Application Support/whisper/small.en/` and set `status = .missing`) but the engine stayed `.whisperMLX`, and recording *still* produced a Whisper transcript because (a) `Tunings.engine` and `WhisperModelStore.status` were fully decoupled and (b) `WhisperMLXTranscriber.resolveLocation()` silently fell back to `.bundled`, whose weights this sideload build still shipped — so it transcribed off the bundled copy with no record-time guard (that was T1.2f, unbuilt). **Fix (chosen UX: "can't select Whisper without a model"):** single-source invariant `Tunings.reconcileEngineAvailability(whisperReady:)` — reverts a `.whisperMLX` selection to `.apple` whenever the model isn't ready (Apple is always available). Enforced at the three transition points: (1) the `SettingsView` Whisper engine row is `.disabled` + shows "Download the model below to enable" unless `status == .ready`; (2) `WhisperModelSection` gained an `onDeleted` callback that SettingsView wires to `reconcileEngineAvailability(whisperReady: false)`, so deleting reverts the selection in the same gesture; (3) `ContentView.task` reconciles a persisted `.whisperMLX` against `whisperStore.status` before constructing the view model, so a selection can't outlive a deleted model across launches. No separate record-time error path — gating + reconcile keep `engine == .whisperMLX ⟹ model ready`. **Tests:** 3 new in `TuningsPersistenceTests` (reverts when not ready, keeps when ready, leaves Apple untouched). 71 tests / 12 suites green.
- **Stopped bundling the 481 MB Whisper weights — download-only; built `.app` dropped ~555 MB → ~74 MB.** The T1.2 arc's end state: weights come only from `WhisperModelStore`'s download into Application Support, never the bundle. Root cause of the delete-still-records confusion above was that the sideload `.app` shipped a *second* full copy of the weights. Removed it from Copy Bundle Resources via a `PBXFileSystemSynchronizedBuildFileExceptionSet` membershipExceptions entry (`Relay Notes/Resources/whisper-small.en/weights.safetensors`, beside the existing `Info.plist` exception). The **small** staged assets (`config.json`, `gpt2.tiktoken`, `mel_filters.safetensors`) + `ls_test.flac` fixture stay bundled — `stageBundledAssets` still copies the three small files at download time, and `.bundled` resolves them; only `WhisperModel.load(from: .bundled)` (the weights load) no longer works. **Reworked the `.bundled`-weights consumers:** `MLXSmoke` resolves the downloaded model once (`resolveDownloadedModelLocation()` → `store.location` when `.ready`, else `nil`) and its three model-loading sections (`runWhisperModel`/`runWhisperTranscribe`/`runWhisperChunked`) take an optional `WhisperModelLocation` and skip with a "download from Settings first" hint when absent; the device-gated `assetsAreCachedAcrossCalls` test loads from a `.ready` store (skips cleanly otherwise). Verified on the simulator build: `find "<app>" -name weights.safetensors` → absent, the three small files + flac → present. Docs updated (`WhisperModelLocation`, `WhisperMLXTranscriber.fallbackLocation`, CLAUDE.md "Whisper weights are download-only"). 71 tests / 12 suites green; **device re-validation pending** (re-download → record → delete → confirm Whisper is no longer selectable and recording is blocked; MLXSmoke loads from the downloaded model).

## 2026-06-12

- **T1.2f recorder placeholder UX shipped — Whisper recordings show a placeholder card (text + live mic meter + elapsed) instead of a perpetually blank live-transcript card; T1.2 is now functionally complete (device pass pending).** Closes the last open half of T1.2f (the missing-model gating half shipped 2026-06-11). Whisper emits zero partials by design, so the live transcript card sat empty for the whole recording — no feedback that the mic was even live. **Session-authority signal:** new `TranscriptionSession.emitsLivePartials` (`AppleSpeechSession` → `true`, `WhisperStreamingSession` → `false`) — the session knows its own decode model, so the recorder asks it rather than re-deriving from the `TranscriptionEngine` enum (same "session is the authority" stance as `modelDescription`). Captured once at record start into `RecorderViewModel.emitsLivePartials`, so it's stable across the recording even if `tunings.engine` were changed mid-session, and still readable through `.finalizing`. **`RecorderView` branch:** during `.recording`/`.paused`, streaming engines keep the existing live card; non-streaming engines get `whisperPlaceholderCard` — "Transcript will appear when you stop recording." + a new `AudioLevelMeter` (proportional `Capsule` fill) + a `M:SS` elapsed label. The `.finalizing` spinner now reads engine-aware copy: "Transcribing…" for Whisper (the decode genuinely happens at finish) vs "Finalizing…" for Apple. **View-model plumbing (Apple path left byte-for-byte unchanged):** `audioLevel` is computed in the existing feed loop via RMS over each fed Float32 buffer, but `updateAudioLevel` early-returns for streaming engines — no per-buffer work, no `@Observable` churn for Apple; `elapsed` is driven by a pause-aware `ContinuousClock` ticker (`elapsedTask`) that banks real clock deltas only while `state == .recording` (so interruption pauses freeze it, drift-free, with no transition wiring) and is cancelled alongside the other tasks in `stopAndTranscribe` / `cleanupAfterFailure`; both only start for `!emitsLivePartials`. Meter mapping (`normalizedLevel`, `−50 dB` floor → 0...1, fast-attack/slow-decay smoothing) and `formatElapsed` are pure `nonisolated static` helpers. **Tests:** new `RecorderPlaceholderTests` (7) pinning the two pure helpers (elapsed M:SS incl. sub-second truncation + negative clamp; level floor/cap/monotonicity) and the Whisper zero-partials authority signal; wired via `scripts/add_test_file.rb`. 78 tests / 13 suites green on the iPhone 17 Pro simulator. **Device validation pending** — the placeholder path can't be exercised on the simulator (selecting Whisper is gated on a downloaded model, and MLX crashes the simulator), so the on-device "record with Whisper → see placeholder + moving meter + ticking elapsed → stop → transcript saved" pass is the remaining manual step before T1.2f/T1.2 close.
- **T1.2f + T1.2 device-validated and closed (iPhone 15 Pro Max).** Recorded with Whisper selected: the placeholder card rendered in place of the (blank) live-transcript card with a **moving mic meter** and **ticking elapsed**, stop showed **"Transcribing…"**, and the transcript saved to the `Note`. That's the T1.2f done-when's last clause — flipped `[x]`. With every substage T1.2a–T1.2f now shipped + device-validated, **T1.2 ("Whisper wired into the recorder flow") is closed**; Whisper is a fully wired daily-driver engine alongside Apple Speech. Remaining in the T1 slice: **T1.3** (1-min/5-min load/decode/memory/battery measurements + decisions log).
- **Refactored `Tunings` + Settings into shared-vs-engine-specific groups (Approach C) — [issue #3](https://github.com/AlteredCraft/relay-notes/issues/3); pays off debt from the Apple-only era.** `Tunings` was built when Apple Speech was the only engine, so the Apple-specific recognition dials (`preset`, `contextualStringsText`) sat as flat properties and the Settings page showed the *flat union* of every engine's controls — under Whisper, Preset + Contextual biasing rendered as live no-ops (editable, persisted, read by nothing, since `transcriptionOptions` returns bare `.whisperMLX`). **Data model:** moved the Apple dials into an `AppleSpeechSettings` bundle (+ empty, reserved `WhisperSettings`) on `Tunings`, mirroring the `TranscriptionOptions` sum type; `sessionMode` / `bitrate` / `engine` stay top-level (shared). Persistence **reuses the original UserDefaults keys** (`tunings.preset`, `tunings.contextualStringsText`, …) so existing installs (incl. the dogfood device) keep their settings with **no migration** — only the in-memory shape changed; bundle `didSet`s fire on `$tunings.apple.preset`-style binding edits. Public seams (`recordingOptions` / `transcriptionOptions`), the injectable `UserDefaults`, and `reconcileEngineAvailability` unchanged. **UI:** `SettingsView` reordered to engine picker → always-shown **On-device model** (`WhisperModelSection`, kept always-visible so Whisper can be provisioned before it's selectable) → an engine-conditional **Recognition** group that swaps `AppleSpeechSettingsSection` (Preset + biasing) for `WhisperSettingsSection` (explanatory placeholder — Whisper has no v1 decode dials) → shared **Capture** (mode) → **Storage & playback** (bitrate, relabeled away from "accuracy": both engines transcribe live PCM, not the saved `.m4a`, so bitrate is playback-only) → Debug. New section views auto-included by the file-system-synchronized group (no pbxproj edit). `RecorderView.tuningSummary` made engine-aware (leads with engine name; drops the Apple-only preset for Whisper). Docs: `transcription-tuning.md` gained an engine-relevance note + a Decisions-log row and a fix to a stale "recording is blocked" line (the missing-model path shipped as *gating*, not a record-time block); `notes.md` V1.1 table gained an engine-scope bullet. **Tests:** 6 new in `TuningsPersistenceTests` (bundle defaults, legacy-key round-trip pinning the no-migration promise, switch-engine-preserves-Apple-bundle, `transcriptionOptions` carries/ignores the bundle per engine, reset clears bundles). 84 tests / 13 suites green on the iPhone 17 Pro simulator. Behavior-preserving for the Apple path; Whisper users stop seeing inert dials. **Adding the next engine (T2/T3)** is now a bundle + a section view + a `transcriptionOptions` arm + one `SettingsView` case — no new flat fields, no shared-group edits.
- **Delete of the on-device Whisper model now asks for confirmation.** A 480 MB delete that forces a re-download was a one-tap destructive action with no guard. Added a `.alert("Delete the on-device model?")` in `WhisperModelSection` (Delete / Cancel, with re-download-size copy), matching the `NoteDetailView` trash-confirmation convention; the Delete button now sets `showDeleteConfirmation` and the actual `store.delete()` + `onDeleted()` runs only on confirm. UI-only, no logic change. Build clean.
- **Re-transcribe a saved note with an alternate engine — [issue #4](https://github.com/AlteredCraft/relay-notes/issues/4); turns the persisted audio into a debug/tuning asset.** Reframed #4 from "do we need to persist raw audio?" to "keep it as a tuning asset, defer removal until the engine A/B is locked" (grounded finding: neither engine reads the saved `.m4a` — Apple transcribes live PCM, Whisper decodes in-memory PCM at finish; the file's only consumer is `NoteDetailView` playback, so it's *not required*, only useful). **Feature:** `NoteDetailView` gained a "Re-transcribe" menu (engines from `ReTranscriber.availableEngines` — Apple always, Whisper only when its model is `.ready`, mirroring `Tunings.reconcileEngineAvailability`); picking one re-decodes `note.audioURL` and presents a `ReTranscribeOutcomeSheet` (current-vs-new side-by-side) with **Replace / Keep original** — non-destructive until Replace writes back `transcript` + `transcriptionModel`. **First real consumer of the file-based `Transcriber.transcribe(_:options:)` path** (previously only `MLXSmoke` exercised it; the streaming path stays the live recorder's). **New `ReTranscriber` (`@MainActor @Observable`, `Recording/ReTranscriber.swift`):** owns the engine availability + the pure mappings — `options(for:)` (each engine's *default* options, deliberately not the user's live `Tunings`, so an A/B is reproducible), `provenanceLabel(for:)` (sourced from the same constants the live sessions report, so a re-transcribed note is indistinguishable from a freshly recorded one), `userMessage(for:)` (generic/actionable, no internals; `noSpeechDetected` → specific). **Shared factory:** `ContentView` now builds one `TranscriberFactory` and hands it to *both* `RecorderViewModel` and `ReTranscriber`, so a re-run reuses the already-loaded ~481 MB Whisper model instead of a second copy; threaded `reTranscriber` through `NotesListView` → `NoteDetailView` (optional, `nil` in previews → control hidden, also hidden when the audio file is absent, e.g. `SampleNotes`). **Tests:** new `ReTranscriberTests` (9, one parameterized over 4 cases) pinning availability gating, the option/label/error mappings, and disk-presence — all simulator-safe; the actual Whisper re-decode is device-only (MLX) per the gating convention. 92 tests / 14 suites green on the iPhone 17 Pro simulator. **Device validation pending** — the Whisper re-transcribe path can't run on the simulator.
- **Filed three field-trial issues + logged an engine-comparison observation.** From a handwritten field-trial note (`IMG_2253`): opened [#4](https://github.com/AlteredCraft/relay-notes/issues/4) `question` (design: do we need to persist raw audio at all, given transcript-as-source-of-truth + future TTS playback — touches `LiveAudioEngine` dual-write + `Note.deleteWithAudio`), [#5](https://github.com/AlteredCraft/relay-notes/issues/5) `enhancement` (edit transcription text, likely in `NoteDetailView`), [#6](https://github.com/AlteredCraft/relay-notes/issues/6) `bug` (a new recording is hidden by an active search filter — `ContentView.searchText` → `NotesListView.filteredNotes` — so it reads as a failed save; fix by surfacing the filter state or clearing `searchText` on record). Also logged the note's qualitative finding to `notes.md` § B: **Apple Speech ≥ Whisper `small.en` in daily-use dictation** (single-observer impression, not a measured WER) — if the gap holds it weakens the case for Whisper-as-default, so flagged for a controlled same-audio A/B. Skipped two items from the note: an Apple-vs-Whisper line (folded into the observation) and a "Whisper session failed, note missing from list" bug the author retracted as false.
- **Compacted + re-synced the `planning/` docs (`notes.md`, `transcription-tuning.md`).** Both had drifted (the `notes.md` "spine" still showed a one-method `Transcriber` + struct `TranscriptionOptions`; `transcription-tuning.md` listed the model as `whisper-small.en-mlx` "~250 MB" and T1.2 as "in progress") and carried verbose completed-stage ship prose that duplicates this CHANGE_LOG. **Accuracy fixes:** spine corrected to the real two-method protocol + `.apple`/`.whisperMLX` sum type; model corrected to `mlx-community/whisper-small.en-fp16` `model.safetensors` ~481 MB (matches `WhisperModelStore.expectedSize`); T1.2 marked shipped/device-validated; tenses de-futured. **Compaction (keep key learnings + the architectural *why* + future plans; bulk → appendix, referenced from the body):** `notes.md` gained a "Where we are now" snapshot, collapsed the V1.0–T1.2 roadmap prose to done-lines pointing here, and moved the deferred LLM engine/model research + the V1.1 empirical tuning table into Appendix A/B; `transcription-tuning.md` kept the per-dial why + hidden knobs + streaming-vs-file learnings in the body and moved the 20-row decisions log to an "Appendix: Decisions log." Cross-reference anchors verified. 321→251 and 238→186 lines. No code changes.
- **Edit a note's transcript, with revert-to-original — [issue #5](https://github.com/AlteredCraft/relay-notes/issues/5).** Transcription is never perfect; the user can now correct it in place. **Two states by design, not a history:** added `Note.originalTranscript: String?` — `nil` is *pristine* (never edited; the value for every pre-existing note, so no backfill, same lightweight SwiftData migration as `transcriptionModel`), and it holds the verbatim machine baseline once edited. `transcript` always stays the current displayed text. **Edit/revert logic lives on the model so it's unit-testable** (not buried in the view): `applyEditedTranscript(_:)` stashes the machine transcript into `originalTranscript` on the *first* divergence only (later edits keep that baseline), no-ops on an unchanged edit, and returns to pristine if an edit lands back exactly on the original; `revertTranscript()` restores the baseline and clears it; `isEdited == (originalTranscript != nil)`. **`NoteDetailView`:** new toolbar **Edit** button swaps the selectable `Text` for an expanding `TextField(axis: .vertical)` (not `TextEditor` — grows with content and scrolls inside the existing `ScrollView`) and flips the trailing items to **Cancel**/**Done** while hiding the back button (`navigationBarBackButtonHidden`) so an in-progress edit can't be navigated away from; an inline **"Edited · Revert to original"** caption (mirrors the `reTranscribeControl` style) appears only when `isEdited`, and Revert is guarded by a confirmation dialog ("Your edits will be discarded") matching the delete-confirmation convention. **Re-transcribe interaction (issue #5 open question):** the `ReTranscribeOutcomeSheet` "Replace" path now clears `originalTranscript` — a re-transcription is a fresh machine baseline, so the note returns to pristine and a prior hand-edit no longer applies (the sheet already shows current-vs-new before Replace, so this isn't a silent clobber). **Tests:** 7 new in `NoteTests` (pristine default, first-edit capture, baseline-stable across edits, no-op unchanged, edit-back-to-original → pristine, revert restore+clear, revert no-op, edited-baseline persistence round-trip) — added to the existing file, no pbxproj wiring. 99 tests / 14 suites green on the iPhone 17 Pro simulator; app build clean. **Device-validated on the iPhone 15 Pro Max (2026-06-13)** — edit → Done persists, Revert restores the original, and the re-transcribe Replace path clears the baseline as designed; shipped to the dogfood device.

## 2026-06-13

- **T1.3 measurement harness added to `MLXSmoke` (load / decode / peak-memory / battery in one device run).** The smoke already printed model load + 6 s / 39 s decode times; T1.3 needs the four numbers (load, decode, peak resident memory, battery delta) at **1-min and 5-min** lengths, plus the "assert substring" check. **Substring assertion:** `runWhisperTranscribe` now prints `substring check = PASS/FAIL` against `expectedSubstring` ("openly shouldered the burden", matched case-insensitively on the deterministic `ls_test.flac` decode) — turns the eyeballed transcript into a pass/fail. **New `runMeasurements(_:)`:** tiles the ~6.7 s fixture to 60 s and 300 s, writes each to a temp WAV, and decodes through the real file-based `transcribe(_:options:)` path on **one shared transcriber** (so the 60 s pass pays the realistic cold model load + Metal JIT, matching the recorder's single cached instance). Per length it reports decode wall-time, **realtime factor** (audio_s / decode_s), and a two-layer memory picture: **peak `phys_footprint`** sampled every 50 ms during the decode by a concurrent poll `Task` into a `PeakMemorySampler` actor (off-actor sampling, so it captures the GPU-bound decode's true peak — the figure iOS jetsam counts), **plus MLX's own `Memory.snapshot()` decomposition** (active / cache / peak-active, with `GPU.resetPeakMemory()` before each decode so the peak is attributable to that length). The decomposition was added after the first device run showed a ~2.8 GB `phys_footprint` that's mostly MLX's *reusable buffer cache* (which "grows significantly during inference" per MLX's docs and is reclaimable), not live memory — the raw footprint alone conflates the two and reads as alarmingly high. Battery is sampled before/after via `UIDevice.batteryLevel` with `batteryState` printed and an explicit *unreliable-while-charging* caveat (reading the console means tethered ⇒ charging — coarse OS granularity too). Transcript is repetitive by construction (tiled clip) — this measures **cost vs length, not accuracy** (accuracy is the substring check). **Cleanup:** extracted the chunked block's inline WAV writer into a shared `writeWAV(pcm:to:)` (same scoped-writer/header-finalize pattern, deduped). New helpers `residentFootprintBytes()` (`task_info(TASK_VM_INFO)` → `phys_footprint`, the figure Xcode's gauge shows), `formatBytes`, `batteryString`/`batteryDeltaString`/`batteryStateString`. Simulator build + 99-test suite green; the measurement run itself is **device-only** (MLX crashes the simulator). **Still open in T1.3:** run it on the iPhone 15 Pro Max, then write the numbers into `notes.md` § "T1 measurements" + a Decisions-log row, and revisit `small.en` vs a smaller variant if the numbers demand it.
- **T1.3 closed — measured on the iPhone 15 Pro Max, `small.en` retained.** Two device runs (numbers stable across both). **Headline:** decode ~4× realtime, roughly flat across length (60 s note 13.8 s / 4.3×; 300 s note ~80 s / 3.8×) → a 5-min note sits ~80 s on the "Transcribing…" spinner at finalize. Substring assertion PASS (6.7 s clip decodes verbatim). **Memory (the number worth getting right):** OS `phys_footprint` ≈ 2.8 GB, but the MLX decomposition shows that's **mostly reclaimable buffer cache** — at rest MLX active is ~0 with ~2.7 GB cache; during a decode live model settles at **~464 MB** with transient peak-active ~1.6–2.0 GB; cache ~2.2 GB. **Memory is flat across note length (+46 MB from 1-min → 5-min)** — the windowed decode bounds peak by the 30 s window, not duration, which retires the open worry in [#1](https://github.com/AlteredCraft/relay-notes/issues/1). Ran without the `increased-memory-limit` entitlement (re-confirms 2026-06-10). **Decision: keep `small.en`** — numbers don't justify a smaller variant; if jetsam headroom is ever needed (concurrent L1 MLX LLM), the first lever is `MLX.Memory.set(cacheLimit:)`, not a smaller model. **Battery deferred** — both runs read 0.0% because reading `print` over a tether means the device is charging; harness captures it with a caveat, a real number needs an untethered `os_log`/Console.app run (not load-bearing). **Docs:** added `notes.md` Appendix C (full tables + interpretation), flipped the T1.3 roadmap box to `[x]`, updated the "Where we are now" Next line, and added a Decisions-log row + refreshed the Tier-2 status and greedy-decode rationale in `transcription-tuning.md`. T1 slice complete; next is V1.3 dogfooding.
- **T2 kicked off — second on-device engine decided: NVIDIA Parakeet `tdt-0.6b-v2` via raw `mlx-swift`.** Promoted ahead of L1 to validate the runtime-extensibility claim on a smaller problem than an LLM and give an English accuracy ladder above `whisper-small.en`. Chose Parakeet (FastConformer encoder + TDT/RNN-T decoder, 617M params, CC-BY-4.0, `mlx-community/parakeet-tdt-0.6b-v2`) over Qwen3-ASR because: best English WER of the candidates, a **complete MIT mlx-swift reference port already exists** (`FluidInference/swift-parakeet-mlx`, cross-checked against the Python `senstella/parakeet-mlx`, Apache-2.0), every op it needs is native in mlx-swift (LSTM / Conv1d-2d / BatchNorm / rfft / asStrided / rel-pos attention), and weights load straight from safetensors (no npz→safetensors conversion the Whisper port needed). Qwen3-ASR stays the sanity-check (bigger port surface, no Swift reference, autoregressive decode with no TDT frame-skip). Branch `t2-parakeet`; staged T2.1a–e (model port) → T2.2 (generalize the download store) → T2.3 (per-engine gating) → T2.4 (single-live-MLX-engine eviction) → T2.5 (wire end-to-end). Decision rationale row in `transcription-tuning.md`; roadmap expanded in `notes.md`.
- **T2.1a shipped + device-validated — Parakeet config types + weight-load footprint smoke.** `ParakeetConfig.swift` decodes the real `config.json` into the inference subset (d_model 1024 ×24 layers; 128-mel / n_fft-512 / per-feature front-end; **no `preemph` on this checkpoint**; vocab 1024; tdt durations [0–4]) with custom `Codable` inits for the nested `greedy.max_symbols`, optional `preemph`, and defaulted `num_extra_outputs`; 4 `ParakeetConfigTests` pin those branches. `ParakeetSmoke.swift` (DEBUG, `os.Logger`-backed so output survives a crash and is readable untethered in Console.app under subsystem `alteredcraft.Relay-Notes`) downloads the 2.47 GB safetensors, confirms the config parse, dumps the collapsed **42-template weight namespace** (matches the FluidInference key mapper exactly — the input for the encoder/decoder port), and casts F32→bf16 to measure the resident floor. **Device result (iPhone 15 Pro Max): bf16 floor ~1.2 GB (617.87M params), fits without the `increased-memory-limit` entitlement; `loadArrays` is lazy (38 MB after load).** **Load-path finding:** the naive load-then-cast-all path (the reference's `loadParakeetModel`) holds F32 + bf16 at once (~3.7 GB) and OOMs the 8 GB device at the ~3 GB ceiling — so the model load **must cast-and-release incrementally with `GPU.set(cacheLimit: 0)`** (a first buggy `var remaining = arrays` *copy* left the original dict pinning all 697 F32 buffers → jetsam at 3.1 GB; fixed by mutating `arrays` itself). The forward-pass activation peak (T2.1c) is the remaining entitlement question (~1.8 GB headroom over the floor). Also **hardened `DownloadCoordinator`** (widened `timeoutIntervalForRequest` 300 s + `waitsForConnectivity`, made internal) after a transient HF-Xet CDN stall aborted the 2.5 GB download on the default 60 s timeout; T2.2 to add resume + background `URLSession`. Simulator suite green (new config tests + no regressions).
- **T2 implementation plan / handoff doc added — `plan.T2.md` (repo root).** A self-contained continuation guide for the Parakeet port (written for a fresh agent): status table, the T2.0 decision, load-bearing findings (incremental cast-release load, MLX-is-device-only, `os.Logger` durability, HF-Xet download robustness), the 42-template weight key map, featurizer/encoder/TDT-decoder/chunking specifics, provider-spine integration points, the build/test/device-validate loops, stage-by-stage remaining work (T2.1b–e, T2.2–T2.5), a conventions checklist, and open decisions. Surfaces **three featurizer port-fidelity risks** where the Python (`senstella/parakeet-mlx`) and Swift (`FluidInference/swift-parakeet-mlx`) references differ — **preemph** (very likely 0.97, *not* the config-absent `nil` the T2.1a comments assumed — NeMo/senstella default to 0.97; the FluidInference port's `nil` is a likely latent bug), periodic-vs-symmetric Hann, and L1-vs-L2 magnitude — with the T2.1d substring check (`ls_test.flac` → "openly shouldered the burden") as the arbiter.
- **T2.1b shipped (code-complete; device mel-smoke pending) — Parakeet mel front-end `ParakeetAudio.swift`.** Ports `senstella/parakeet-mlx`'s `get_logmel` to mlx-swift: preemph → periodic Hann (win 400, zero-padded to n_fft 512) → STFT → L2 magnitude → power² → Slaney 128-mel → `log(x+1e-5)` → per-feature (per-mel-bin) z-score → `[1, t, 128]`. The genuinely generic, already-device-validated STFT primitives (`WhisperAudio.stft`/`hanning`) are reused; only the window padding, mel, and normalization are Parakeet-specific. **Resolved the plan's three §5.2 risks toward NeMo's training featurizer:** (1) **preemph 0.97** — `dacite.from_dict` (senstella's loader) applies the `PreprocessArgs.preemph=0.97` dataclass default for the *absent* key, confirming the model was trained with it; flipped `ParakeetConfig` to decode absent→0.97 while still honoring a present value (incl. explicit `null`→`nil`, distinguished via `container.contains`), and corrected the T2.1a "preemph off" comments. (2) **periodic Hann** via `WhisperAudio.hanning` (denominator N), not FluidInference's symmetric `/(N−1)`. (3) **L2 magnitude** (`MLX.abs` on the complex rfft), matching NeMo's `torch.stft`, not the Python's `|re|+|im|` L1. **Found + fixed a fourth latent risk the plan didn't enumerate:** the FluidInference filterbank uses the **HTK** mel *scale* (`2595·log10(1+f/700)`), but the oracle calls `librosa.filters.mel(htk=False, norm="slaney")` = the **Slaney** mel scale; `ParakeetAudio.melFilterbank` reimplements the Slaney scale + Slaney area-norm in host `Double` to match librosa exactly (also dodging the reference's per-element `.item()` GPU syncs). Also used the `nFFT`-based STFT frame count (= NeMo's `torch.stft(center=True)`), not the Python's `win_length`-based count (a latent quirk that can read past the buffer on some lengths). **Smoke:** new weight-free `ParakeetSmoke.runFeaturizer()` (T2.1b) computes the mel on `ls_test.flac` and logs shape + range; runs *before* the heavy T2.1a cast so the result lands fast. Side-fix: marked `ParakeetTDTConfig.load(from:)` `nonisolated` (an extension method doesn't inherit the struct's `nonisolated`, so the default-MainActor isolation made it uncallable from the nonisolated smoke — a Swift 6 hard-error-in-waiting). Build + 5 `ParakeetConfigTests` green (added an explicit-`null`-preemph test). **Device-validated 2026-06-13 (iPhone 15 Pro Max):** `runFeaturizer` on `ls_test.flac` (106640 samples / 6.665 s) → mel **`[1, 667, 128]`** (= NeMo's `1 + len//hop` frame count, confirming the `nFFT`-based STFT), **per-feature mean ≈ 0** (8.3e-08 — the z-score working), range [−1.77, 6.79]; `preemph 0.97` logged live. The smoke validates shape + normalization; **full numerical correctness of the four fidelity choices (preemph / Hann / L2 / Slaney scale) still rides on the T2.1d substring gate** — they're the suspects if it fails.
- **T2.1c shipped (code-complete; device forward + peak-footprint pending) — FastConformer encoder.** `ParakeetEncoder.swift` (`ParakeetConformerEncoder` + `ParakeetFeedForward` / `ParakeetConvModule` / `ParakeetConformerBlock` / `ParakeetSubsampling`) and `ParakeetAttention.swift` (`ParakeetRelPosAttention` + `parakeetRelPositionalEncoding`) port `senstella/parakeet-mlx`'s `conformer.py`/`attention.py` to mlx-swift: dw-striding subsampling (factor 8, 128→16 freq, `C·16→1024` projection) → 24 macaron conformer blocks (½-residual SiLU FF → rel-pos MHA → pointwise/GLU/depthwise-k9/BatchNorm/SiLU conv → ½-residual FF → LayerNorm). **Two load-bearing port decisions, both verified against the references + mlx-swift source:** (1) **no weight remapper and no conv transpose** — the `@ModuleInfo`/`@ParameterInfo` keys are the snake_case safetensors keys verbatim (the Whisper-port convention), and the mlx-community safetensors already store conv weights in MLX channel-last layout (the reference loads with `let transformedWeights = weights`, i.e. no transpose); so `loadArrays` → strip `encoder.` → `unflattened` → `update` is the whole mapping. (2) **rel-pos attention via the SDPA-additive-mask trick** — `matrix_bd = relShift((q+bias_v)·pᵀ)·scale` passed as the additive `mask` to `MLXFast.scaledDotProductAttention(q+bias_u, k, v)`, which is the Transformer-XL `AC+BD` sum (only the `rel_pos` variant is ported; the `rel_pos_local_attn` Metal-kernel path is skipped — this checkpoint is `rel_pos`). The positional encoding is a free function (no learned params → stays out of the module tree) computing exactly the centered `2·T−1` window the Python slices from its 5000-row buffer. **Caught a fourth latent issue beyond §5.2's three:** the Python's `win_length`-based STFT frame count can read past the buffer on some lengths; used the `nFFT`-based count (= NeMo's `torch.stft(center=True)`), already in T2.1b. **Loaded by the incremental bf16 cast-release path (§3.1):** `ParakeetConformerEncoder.load` casts each F32 tensor and drops its source before the next with `Memory.cacheLimit = 0`, keeping only `encoder.*` (decoder/joint F32 released); MLX laziness means the modules' random init weights are never materialized before `update` replaces them. **Smoke:** new `ParakeetSmoke.runEncoder()` (T2.1c) loads the encoder, featurizes `ls_test.flac` → bf16, runs the forward, and logs output shape (expect `[1, ~84, 1024]`) + timing + the **peak `phys_footprint`** sampled off-actor during the forward — the figure that decides the `increased-memory-limit` entitlement (§3.1); `run()` now does featurizer→encoder (T2.1a `runLoadFootprint` retained, no longer auto-run, to avoid a redundant second cast). Side: used the non-deprecated `Memory.cacheLimit` setter in new code. Build + full simulator suite green (encoder is MLX-touching ⇒ device-only; no new sim tests). **Device-validated 2026-06-13 (iPhone 15 Pro Max):** encoder output **`[1, 84, 1024]`** (667 mel frames ÷8; exact), output a clean LayerNorm-shaped distribution (range [−0.58, 0.49], mean ≈ 0 — strong evidence the 42 key templates all loaded; the final proof is the T2.1d substring gate), forward **130 ms** (~50× realtime), load 1.85 s. **Entitlement resolved (open question #2): NOT needed** — forward-pass peak `phys_footprint` **1.31 GB** (MLX peak-active 1.22 GB), only ~60 MB of activations over the ~1.27 GB bf16 weight floor (weight-dominated), ~1.7 GB under the ~3 GB no-entitlement ceiling. Parakeet runs without `increased-memory-limit`, like Whisper, with room for the tiny decoder/joint in T2.1d.
- **T2.1d shipped (code-complete; device substring gate pending) — TDT greedy decoder + vocab decode + full-model wiring.** `ParakeetDecoder.swift` (`ParakeetPredictNetwork` = embed + 2-layer LSTM stack; `ParakeetJointNetwork` = enc/pred projections → ReLU → final Linear; `ParakeetTDTModel` = encoder + decoder + joint) and `ParakeetTokenizer.swift` (`parakeetDecodeTokens`, `▁`→space) port `senstella/parakeet-mlx`'s `rnnt.py` + `parakeet.py::decode_greedy` + `tokenizer.py`. **The TDT greedy loop:** per encoder frame, run the prediction net (embed last emitted token or zero; advances state **only on a non-blank emission**) + joint → split the joint logits into the **vocab head** (`argmax`; index == vocab size ⇒ blank ⇒ don't emit) and the **duration head** (`argmax` over the 5 `durations` ⇒ frames to advance `step`), with the `max_symbols=10` stuck-guard forcing progress on a run of duration-0 steps. **Verified against mlx-swift source so the weights load with no remapper:** mlx-swift's `LSTM` already exposes the `Wx`/`Wh`/`bias` keys and the i/f/g/o gate order the mlx-community checkpoint was converted with (per-layer LSTMs load directly into `dec_rnn.lstm.{N}.*`); the `joint_net` array keeps `[ReLU, Identity, Linear]` so the final Linear lands at index 2 (`joint_net.2.*`); the `prediction` sub-container nests `embed`/`dec_rnn` to match `decoder.prediction.*`. Single-step decode (batch 1, seq 1) sidesteps the reference LSTM wrapper's seq/batch-axis ambiguity. The lazy graph is bounded by `eval`-ing the committed LSTM `(h, c)` each emission. `ParakeetTDTModel.load` loads the **full** safetensors (encoder + decoder + joint) via the §3.1 incremental bf16 cast-release, keys verbatim. **Smoke:** `ParakeetSmoke.runDecode()` (T2.1d) loads the full model, featurizes `ls_test.flac`, transcribes end-to-end, and asserts the transcript contains *"openly shouldered the burden"* (case-insensitive) — logs PASS/FAIL + transcript + the full-model peak footprint; `run()` now does featurizer→decode (subsuming the encoder load). 3 sim-safe `ParakeetTokenizerTests` (▁→space, out-of-range guard, empty) wired via `add_test_file.rb`. Build + full simulator suite green. **Device-validated 2026-06-13 (iPhone 15 Pro Max): substring PASS** — `ls_test.flac` → *"Then the good soul openly shouldered the burden she had borne so long in secret, and bravely trudged on alone."*, word-matching the Python reference; transcribe 287 ms (~23× realtime), full-model peak `phys_footprint` 1.34 GB (still no entitlement). **This confirms the entire T2.1 model port (featurizer + encoder + decoder) end-to-end.** Getting there took fixing two bugs that only surfaced past a byte-correct encoder, both isolated **against the Python oracle on the Mac (no device round-trips)**: (1) **BatchNorm ran in training mode** — MLXNN Modules default to `training = true`, so the conv BatchNorm used batch stats instead of the loaded running_mean/var; the loaders now call `model.train(false)` (the Python `model.eval()`). Insidious because training-mode BatchNorm still produces *normalized-looking* output, so the T2.1c shape/range smoke passed while the encoder was numerically wrong. (2) **the joint's final Linear silently never loaded** — `let jointNet: [Module]` keyed off the property *name* (`jointNet`), but the safetensors key is `joint_net` (MLXNN has no `@ModuleInfo` key-override for unwrapped arrays — the encoder's `layers`/`conv`/`lstm` only match because they're single words); `update(verify: .none)` skipped the unmatched `joint_net.2.*`, leaving a **random final projection** → real-but-wrong tokens. Renamed the property to `joint_net`, and **switched both loaders to `update(verify: .noUnusedKeys)`** so any future key mismatch throws at load (naming the unused weight) instead of silently degrading. The decisive diagnostic: reimplementing our *exact* Swift decode loop in Python on the reference weights produced the *correct* transcript, proving the decode logic was right and pinning the bug to mlx-swift loading. The earlier L2→L1 magnitude fix (T2.1b CHANGE_LOG / §5.2 RISK 3) was found the same way — diffing the featurizer against the oracle. Device confirms the featurizer (`mel[0,0,:5]`) and full encoder (`enc[0,0,:5]`, range) now reproduce the reference to bf16 precision.
- **T2.1e shipped (code-complete; device chunk-smoke pending) — long-audio chunking (overlap windows + token merge).** `ParakeetChunking.swift` (new) adds the time-aligned token type `ParakeetToken` (`id`/`text`/`start`/`duration`) and `ParakeetChunkMerge` (`longestContiguous` → `longestCommonSubsequence` fallback + a shared midpoint `cutoff` and `stitch`), porting `senstella/parakeet-mlx`'s `alignment.py` (`merge_longest_contiguous` + `merge_longest_common_subsequence`) **verbatim** — the FluidInference Swift port ships only a simplified cutoff stub that drops/dups boundary words (plan.T2.md §5.5). The merge is pure integer-id + `Double`-timestamp math (no MLX), so it's **simulator-safe and unit-tested directly**: 6 `ParakeetChunkingTests` pin no-overlap concat, thin-overlap cutoff, contiguous dedup, and the non-contiguous LCS fallback (gap token preserved, b-tail appended, no dup) — all green on the simulator. In `ParakeetDecoder.swift`: `ParakeetTDTModel` gains `timeRatio` (= `subsamplingFactor·hopLength/sampleRate` = 0.08 s/frame, senstella's `time_ratio`); the greedy loop moved to `decodeGreedyAligned` returning `[ParakeetToken]` with `start = step·timeRatio` / `duration = durations[decision]·timeRatio` (the pre-advance `step`, matching the reference), and `decodeGreedy` is now a thin `.map(\.id)` wrapper (single-pass numerics unchanged). New `transcribeChunked(audio:preprocessor:filters:chunkDuration:overlapDuration:)` ports `parakeet.py::transcribe(chunk_duration=…)`: a whole-clip fast path when the audio fits one window (byte-identical to `transcribe(_:)`), else step the PCM in `chunkDuration` windows at stride `chunk-overlap`, featurize+encode+decode each independently, offset token timestamps by the window start, and merge the overlap (defaults 120 s / 15 s, the reference's). **Why chunking at all:** the encoder's attention is O(T²) and a multi-minute note would also grow activations past the no-entitlement ceiling — windowing bounds both. **Smoke:** `ParakeetSmoke.runChunked()` (T2.1e) tiles `ls_test.flac` ×6 (≈40 s) and transcribes it two ways from one model — whole-clip vs chunked at a deliberately small 15 s window / 5 s overlap (≈4 windows) — then compares; PASS = chunked contains the expected substring **and** matches the whole-clip output (byte-identical, or occurrence-count-preserved). The tiled clip is adversarial for the merge (identical sentence repeated → the overlap is full of equal ids), which is exactly what the timestamp proximity guard (`|Δstart| < overlap/2`) disambiguates. `run()` now does featurizer→decode→chunked. Build + full simulator suite green; **the overlap merge is device-pending only for the end-to-end tiled-clip smoke** (the merge math itself is unit-validated). This is the last T2.1 stage — the Parakeet model port is complete; next is the provider-spine wiring (T2.2–T2.5).
- **T2.1e device-validated (iPhone 15 Pro Max) — chunking PASS; whole T2.1 model port complete.** `ParakeetSmoke.runChunked` on a tiled ×6 (≈39 s) `ls_test.flac`: chunked transcribe at an aggressive 15 s window / 5 s overlap (≈4 windows) produced **6/6 complete, correctly-ordered sentences with no dropped or duplicated words at any boundary** (occurrence count 6 == whole-clip) — the overlap merge is correct. The single difference from the whole-clip pass — one within-chunk word, "trudged"→"troubled" in the 4th sentence — sits **past the overlap region** (decoded by a single chunk, the merge uninvolved), so it's edge-context recognition variance from the deliberately tiny smoke window, not a merge defect; it's largely moot at the 120 s/15 s production default. Timing: whole-clip 2.34 s vs chunked 3.39 s for 39 s (the 50 %-overlap smoke setting re-encodes more; ~12 % redundancy at the production overlap). The smoke's tiered check (`byte-identical` → `occurrence-count-preserved` → `substring-present`) reported PASS at the count-preserved tier, exactly as designed. **This closes T2.1 — the Parakeet featurizer + FastConformer encoder + TDT decoder + long-audio chunking are all on-device-validated.** Next: provider-spine wiring (T2.2 generalize the download store → T2.5 end-to-end engine selection).
- **T2.2 shipped (code-complete; fresh-download integrity device-pending) — generalized the download store to `DownloadableModelStore(spec:)`.** Extracted the generic machinery from the Whisper-only `WhisperModelStore` into a spec-driven `DownloadableModelStore` (new `DownloadableModelStore.swift`): a `ModelDownloadSpec` = `{ subdirectory, remoteFiles [{url, sha256, size, destFilename}], bundledFiles [{name, ext}], downloadSizeMB }`, two pinned specs (`.whisperSmallEn`, `.parakeetTDT06bV2`), and the `DownloadCoordinator`. The store now downloads **N remote files** (each SHA-256 + byte-size verified, atomically installed, excluded from iCloud backup), stages any bundled assets, and reports **byte-weighted overall progress** across files; readiness = all remote files present. `WhisperModelStore` and the new `ParakeetModelStore` are **thin subclasses** binding the spec (no stored props ⇒ observation unaffected) — so every existing `WhisperModelStore()` call site, static accessor (`downloadURL`/`expectedSHA256`/`expectedSize`, now forwarding to the spec), and `WhisperModelStoreTests` keep working unchanged (only `WhisperModelSection.failureMessage`'s param type moved to `DownloadableModelStore.FailureReason`). **Pinned the Parakeet manifest to commit `b8e276dc…`:** `model.safetensors` sha256 `b958c37a…` / 2 471 559 904 B (= the device's 2357 MB; the Git-LFS oid from HF's tree API) and `config.json` sha256 `9bd323e6…` / 36 176 B (computed from the pinned bytes — config.json isn't LFS). **§3.4 download robustness — resume/retry added:** on a transient failure `DownloadCoordinator` now re-issues the request up to 5×, **resuming from `NSURLSessionDownloadTaskResumeData`** when the server supports range requests (HF Xet/S3 does) so a mid-transfer stall doesn't discard the bytes already on disk, else restarting the file; the typed `FailureReason`→generic-actionable-message UX is reused as-is. **`ParakeetSmoke.ensureModelDownloaded` now uses the real `ParakeetModelStore`** (SHA-verified, resume-on-stall) instead of its throwaway `URLSession` fetch — dropping the `Synchronization`/`Mutex` progress hack and the un-pinned `resolve/main/` URLs; a poll loop logs coarse progress. 6 new sim-safe `DownloadableModelStoreTests` (spec pinning + sizes, multi-remote-file readiness, multi-file delete, subdirectory composition) + the unchanged `WhisperModelStoreTests` (via the subclass) are green; build + full simulator suite (17 suites) green. **Scope decision — true background `URLSession` deferred** (plan §3.4 / open-Q #6): for a one-time, app-foregrounded sideload download, resume-on-stall covers the actual observed failure (the -1001 mid-transfer stall), and a background session's app-relaunch lifecycle (persisted state, `handleEventsForBackgroundURLSession`, continuation that doesn't survive suspension) is disproportionate complexity for v1; recorded as a follow-up. Remaining T2: provider-spine wiring (T2.3 per-engine gating → T2.4 single-live-MLX eviction → T2.5 end-to-end).
- **T2.2 device-confirmed (iPhone 15 Pro Max) — store-wired smoke green.** Re-ran `ParakeetSmoke` through the new T2.2 code path: `ParakeetModelStore` resolved `Application Support/parakeet/tdt-0.6b-v2/`, detected readiness via the multi-file check (both `model.safetensors` + `config.json` present → skipped download with the new "already present" message), and the full pipeline still passed — T2.1b mel `[1, 667, 128]`, T2.1d substring **PASS**, T2.1e chunking **PASS** (6/6 sentences, same benign edge-context word diff), full-model peak `phys_footprint` 1.32 GB (no entitlement). Confirms the spec-driven store wiring works on device. Only the *fresh* download + SHA-256 path stays device-unrun (the device had the files, so it hit the `.ready` fast path); exercise it by deleting the bundle on-device and re-running.
- **T2.2 fully device-validated (iPhone 15 Pro Max) — fresh download + integrity confirmed.** Deleted the on-device Parakeet bundle via the DEBUG "Delete Parakeet model" button, then re-ran the smoke: `ParakeetModelStore` saw `.missing`, **re-downloaded the 2.5 GB `model.safetensors` + `config.json` fresh, SHA-256 + size verified each, installed**, and the full T2.1b/d/e pipeline ran green (substring PASS, chunking PASS). This closes T2.2 — the store's `.missing`→`.downloading`→`.ready` flow, the pinned-hash integrity gate, and the §3.4 resume-on-stall all confirmed on real hardware (not just unit tests). **T2.0–T2.2 are now done + device-validated; the remaining T2 work (T2.3 per-engine gating → T2.4 single-live-MLX eviction → T2.5 end-to-end wiring) is all provider-spine, no more MLX numerics.** A throwaway DEBUG delete button remains in `SettingsView` to force re-download; T2.5 should replace it with the real `ParakeetModelSection`.
- **T2.3 shipped — per-engine gating + `ModelStores` registry (retired the single `whisperReady` Bool).** New `Transcription/ModelStores.swift` (`@MainActor @Observable`) is the one place the engine ↔ model-store mapping and per-engine readiness live: `store(for:)` (compiler-enforced switch, `nil` for Apple which has no model), `isReady(_:)` (Apple always; a model-backed engine only when its store is `.ready`), and `readyEngines`. Gating now asks the registry instead of hardcoding `.whisperMLX`, so the second on-device engine (Parakeet, T2.5) drops in by adding a store + a `store(for:)` arm — no new readiness Bool. `Tunings.reconcileEngineAvailability(whisperReady:)` → `(readyEngines:)` (reverts the selection to Apple whenever the chosen engine isn't in the ready set — Apple, having no model, is always present). `ReTranscriber` now takes `stores: ModelStores` (dropping `whisperStore`); `availableEngines` = `allCases.filter { stores.isReady($0) }`, and the redundant `isAvailable(_:whisperReady:)` static was removed (its logic is `ModelStores.isReady`). `SettingsView` takes `stores` (engine-button readiness via `stores.isReady(.whisperMLX)`, model section via `stores.whisper`, delete reconcile via `stores.readyEngines`); `ContentView` constructs one `ModelStores` and threads it to the factory, `ReTranscriber`, and `SettingsView`, and the launch reconcile reads `stores.readyEngines`. New 9-test `ModelStoresTests` is the single source of truth for the readiness matrix (moved off `ReTranscriber`); `ReTranscriberTests`/`TuningsPersistenceTests` updated to the registry/new signature. Build + full simulator suite green. No behavior change for the user yet (still Apple + Whisper) — this is the structural pivot that makes T2.5's Parakeet selection a data addition.
- **T2.4 shipped — `TranscriberFactory` keeps at most one live MLX engine (evict on switch).** The factory's per-engine instance cache split: Apple Speech (cheap, not MLX) stays cached independently, while the two MLX engines share a single `liveMLX: (engine, transcriber)?` slot. Requesting a *different* MLX engine now drops the prior one's instance (the factory's only strong ref → its ~GB of weights + the actor release) **before** constructing the new one, so Whisper (~0.5 GB) and Parakeet (~1.2 GB) are never co-resident on the 8 GB device; re-requesting the *same* MLX engine returns the cached instance (no reload), and toggling Apple↔an MLX engine doesn't churn the loaded model. Factory dependency switched from `whisperModelStore: WhisperModelStore?` to `stores: ModelStores?` (so T2.5's Parakeet arm gets its store from the same registry); `ContentView` + `WhisperMLXTranscriberTests` updated. Two new `TranscriberFactoryTests` (same-MLX-engine caching; MLX instance survives an Apple detour) — the actual Whisper↔Parakeet eviction gets its test once Parakeet's enum case exists (T2.5). With only Whisper as an MLX engine today the eviction never fires yet; this lands the mechanism. Build + full simulator suite (19 suites) green.
- **T2.5 shipped (code-complete; device end-to-end pending) — Parakeet wired end-to-end behind the provider spine.** Parakeet is now a first-class `TranscriptionEngine` selectable in Settings, recordable, and a re-transcribe target — by adding arms to the existing sum types, not special-casing. The full surface: `TranscriptionEngine.parakeetMLX` (+ "On-device (Parakeet)" displayName) and `TranscriptionOptions.parakeetMLX`; new **`ParakeetMLXTranscriber`** (actor, mirrors `WhisperMLXTranscriber`: caches the §3.1 incremental-cast-release `ParakeetTDTModel` + mel filterbank as `LoadedAssets`, resolves the model dir per-call from the injected `ParakeetModelStore`, drives `transcribeChunked` at the 120 s/15 s production window via shared `transcribePCM`) and **`ParakeetStreamingSession`** (accumulate-PCM-then-decode, `emitsLivePartials = false` → recorder placeholder card, throws `noSpeechDetected` on empty); `Tunings` gains an (empty, v1) `ParakeetSettings` bundle + the `.parakeetMLX` options arm + reset; `ModelStores` gains the `parakeet` store + `store(for:)` arm (so per-engine gating now covers Parakeet for free); `TranscriberFactory` gains the `.parakeetMLX` arm through the **same single-live-MLX eviction path** (so selecting Parakeet evicts Whisper and vice-versa — the T2.4 mechanism now actually fires); `ReTranscriber` gains the `.parakeetMLX` options + provenance arms (the `NoteDetailView` re-transcribe menu picks it up automatically via `availableEngines` + `displayName`). UI: new **`ParakeetModelSection`** (real download/progress/delete/retry section driven by `ParakeetModelStore.status`, ~2.4 GB copy) **replacing the throwaway DEBUG "Delete Parakeet model" button**, and **`ParakeetSettingsSection`** (empty-state placeholder); `SettingsView` adds the Parakeet engine button (disabled until ready) + model section + recognition arm. Added `TranscriptionError.modelUnavailable` (defensive — Settings gating means a Parakeet session only starts when the store is `.ready`; maps to the recorder's generic message). Factored the `FailureReason`→user-copy mapping into `DownloadableModelStore.userFacingMessage` (shared by both model sections; `WhisperModelSection.failureMessage` forwards, keeping its tests). **Provenance:** `ParakeetMLXTranscriber.modelDescription = "Parakeet (tdt-0.6b-v2)"`, persisted on the `Note` identically for fresh records and re-transcribes. Tests: new sim-safe `ParakeetStreamingSessionTests` (location resolution incl. nil-when-no-store / re-resolve-on-delete, session format/provenance/feed/cancel/zero-partials) wired via `add_test_file.rb`; extended `TranscriberFactoryTests` (Parakeet arm + the now-real Whisper↔Parakeet **eviction** test), `ModelStoresTests` (Parakeet readiness incl. both-files-required), `ReTranscriberTests` (Parakeet options/provenance), `TuningsPersistenceTests` (Parakeet options/round-trip/reset). Build + full simulator suite (20 suites) green. **Device-pending (the T2 done-when):** select Parakeet on the iPhone 15 Pro Max, record → transcript; re-transcribe an existing note with Parakeet; confirm the provenance label — plus the accuracy-ladder A/B (open-Q #5) now unblocked. This completes T2's provider-spine wiring.
